# سواپ سولانا — پیکر مدرن توکن، برداشتن دیوار RPC و هوش مصنوعی توکن

> ۲۰۲۶-۰۹-۲۳ · شاخه: `arena/01a0cecf-fbtcryp` · هدف: merge به `main` → deploy خودکار Vercel روی `https://fbtswap.ir`

## خلاصهٔ یک‌خطی

سه گزارش، سه راه‌حل در یک PR: ❶ وقتی آدرس mint جای‌گذاری می‌شود، موجودی با «—» و لیست ۸ میزبانِ ردشده نشان داده می‌شد؛ ❷ باکس انتخاب توکن یک `<select>` لخت بود — بدون لوگو، بدون جستجو؛ ❸ توکن تاییدشده/تحلیل هوش مصنوعی وجود نداشت.

## ❶ دیوار RPC — «هیچ‌کدام از نودها این درخواست را نپذیرفتند»

گزارش واقعی: `publicnode: RPC_ERROR · solanatracker: RPC_ERROR · drpc: HTTP_400 · leorpc: UNREACHABLE · magicblock: BLOCKED · onfinality: RATE_LIMITED · api.mainnet(—beta): BLOCKED`

این یک تصمیم دربارهٔ **تماس‌گیرنده** است (IP، اپراتور، منطقه) — نه خطای قابل ریترای. زیرساختش از قبل بود (رلهٔ read-only روی `POST /api/solana/rpc` و اندپوینت‌های `/api/solana/balances` و `/api/solana/token-info`)، اما در مسیر موجودی به‌درستی وصل نبود:

| تغییر | فایل |
|---|---|
| رلهٔ JSON-RPC خود اپ به فهرست کاندیدهای **خواندن** اضافه شد (`relay: true`) — روی شبکهٔ سالم عقب می‌ماند، روی شبکهٔ مسدود **اول** می‌آید | `src/lib/solana/balanceSource.js` |
| وقتی hint ذخیره‌شده می‌گوید «نودهای عمومی این مسیر را رد کرده‌اند»، درب دوم (سرور خودمان) **بدون تأخیر ۱٫۲ ثانیه‌ای** همزمان رقص می‌شود | `src/lib/solana/balanceSource.js` |
| وضعیت درب دوم روی صفحه نمایش داده می‌شود — «سرور خود اپ (کد): این مسیر هم نخواند» — تا معلوم باشد دومی اصلا امتحان شده یا نه | `src/pages/SolanaSwap.jsx` + ترجمهٔ `solana.serverDoor` |
| متن `RPC_BLOCKED` حالا می‌گوید اپ خودکار از سرور خودش عبور می‌دهد | `fa.json` / `en.json` |

نتیجه (تست `test/solana-balance-doors.test.js`): با ۸ میزبانِ ۴۰۳-دهنده، خواندن موجودی از درب سرور **موفق** می‌شود و به‌جای «—» عدد واقعی می‌نشیند.

## ❷ پیکر مدرن توکن (لوگو + تم)

`<select>` حذف شد؛ دو دکمهٔ توکن (لوگو + نماد + نشان وضعیت) شیت جدید `SolanaTokenPicker` را باز می‌کنند:

- **چشمهٔ داده**: اندپوینت جدید `GET /api/solana/token-search` (فهرست کلید-لس Jupiter از سمت سرورِ خودمان — بدون کلید در کلاینت، بدون CORS، با کش لبه). توکن‌های کیوریتد هم با **یک درخواست گروهی** (`?mints=a,b,c`) لوگوی واقعی می‌گیرند.
- **چسباندن آدرس = جستجو**: کوئری به شکل base58 مستقیم به‌عنوان mint حل می‌شود — در ایندکس هست → نام/لوگو/قیمت؛ نیست → ردیف صادقانهٔ «توکن ناشناس» با مونوگرام گرادیانی (هرگز دایرهٔ خالی) و بلافاصله قابل سواپ.
- **حالت شکست**: هر چیزی که نیامد، پیکر همان رفتار قدیمی «وارد کردن با آدرس» را دارد — خطای ظاهری هرگز به صفحهٔ پول خطا تزریق نمی‌شود.
- اسکلتون شیمر، تم روشن/تاریک با متغیرهای خود اپ، RTL-safe.

نظم فیشینگ حفظ شده: نشان «تایید شده/تاییدنشده» در لیست **و** کنار نماد روی کارت سواپ می‌ماند؛ توکن بدون متادیتا نماد بریده‌شدهٔ آدرس را نشان می‌دهد، نه اسمی که هیچ‌کس تایید نکرده.

## ❸ تاییدشده + هوش مصنوعی توکن

- نشان سبز «✓ تایید شده» برای توکن‌های کیوریتد و توکن‌های `isVerified` ایندکس Jupiter؛ کهربایی «تاییدنشده» برای بقیه.
- `GET /api/solana/token-sentiment?mint=` دو لایه با قرارداد صداقت دارد (مستند در `server/solanaTokenMeta.js`):
  1. **امتیاز قطعی ۰–۱۰۰** از واقعیت‌های منتشرشده: نقدینگی، organicScore، اختیار ضرب/فریز، تمرکز نگه‌دارنده‌ها، سهم سازنده، عمر توکن، نوسان ۲۴س. هر راننده با **کلید ترجمه + عدد** برگردانده می‌شود.
  2. **یک جملهٔ LLM** فقط وقتی کلید AI تنظیم باشد، فقط توصیفِ اعداد، بدون توصیهٔ خرید/فروش، همیشه با برچسب «تحلیل هوش مصنوعی» کنار امتیاز قطعی — جمله هرگز امتیاز را تغییر نمی‌دهد.
- دادهٔ غایب = `unknown`، نه یک نشان سبزِ از خودساخته (همان قاعدهٔ fail-closed `tokenRisk.js` و `solanaIntel.js`).
- نکتهٔ سه‌حالته: ردیف audit یو‌اس‌دی‌کو بولین‌های ضرب/فریز را اصلا گزارش نمی‌کند؛ «گزارش‌نشده» هرگز به «فعال» فرو نمی‌ریزد (تست پین شده).

## تست‌ها

- `test/solana-token-meta.test.js` — ۱۱ تست (نرمال‌سازی، سه‌حالته، fail-closed، ریسک/سلامت با راننده‌های دقیق).
- `test/solana-balance-doors.test.js` — ۳ تست (درب دوم موفق، بدون تأخیر هنگام مسدودی، رله در کاندیدهای خواندن).
- `test/screens.jsx` باندل و اجرا شد: **۲۴۹/۲۴۹** (مونت همهٔ صفحات در ۱۲ زبان).
- `test/wiring.mjs`: همان ۱۴ شکستِ از-قبل‌موجودِ base، **صفر** مورد جدید.
- `npm run build` سبز.

## عملیات

- برای ایندکس‌شدن متادیتا/سنتیمنت نیازی به کلید نیست (lite-api). اگر `OPENROUTER_API_KEY`/`GROQ_API_KEY`/… تنظیم باشد، جملهٔ هوش مصنوعی خودکار فعال می‌شود؛ نبودش فقط یعنی «جمله‌ای نیست»، امتیاز قطعی همچنان هست.
- کش‌ها: سرچ ۳ دقیقه (SWR)، سنتیمنت ۳۰ دقیقه، آیکن گروهی ۳ دقیقه. هیچ‌کدام روی قیمتِ امضا اثر ندارند — قیمت از اندپوینت‌های زندهٔ سفارش می‌آید.
