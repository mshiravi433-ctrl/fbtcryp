# سئوی دوزبانه + رفع چشمک‌زدن صفحهٔ فارسی — ۲۰۲۶-۰۹-۲۴

دو خواسته بود: «سئو را برای فارسی و انگلیسی خیلی زیاد کن تا در جست‌وجوی سواپ، کریپتو و
سرمایه‌گذاری اول باشیم» و «صفحهٔ لندینگ مخصوصاً در فارسی چشمک‌زن است و انگار باگ دارد».

مورد دوم اول اندازه‌گیری شد، چون «چشمک» یک توصیف است و نه یک عدد. آن‌چه پیدا شد باگِ
چیدمان نبود — صفحه از یک موتور واقعی خواسته بود **۴۵ انیمیشن را هم‌زمان زنده نگه دارد**.
اکنون ۸ تاست و نرخ فریم از ۱٫۱ به ۵۷ رسیده. جزئیات عددی پایین‌تر است.

---

## ۱. چشمک‌زدن صفحهٔ فارسی: علت، نه حدس

### ۱٫۱ چه چیزی اندازه‌گیری شد

یک Chromium واقعی (headless shell) روی همان `dist/` ساخته‌شده، با نمای موبایل
(۳۹۰×۸۴۴)، و سه اندازه‌گیری خام:

| اندازه‌گیری | روش |
|---|---|
| فهرست انیمیشن‌های زنده | `document.getAnimations()` گروه‌بندی‌شده بر اساس `animationName` |
| نرخ فریم بی‌کار | ۹۰ فریم `requestAnimationFrame` پشت‌سرهم، میانگین فاصله |
| جابه‌جایی چیدمان | `PerformanceObserver` روی `layout-shift` |

نتیجهٔ خط پایه (قبل از این تغییرات):

```
۴۵ انیمیشن زنده، که ۲۰ تای آن‌ها «لایهٔ تمام‌صفحه» بودند:
  ۶ × ken-burns   ← شش عکس تمام‌قد که بی‌نهایت زوم می‌شدند
  ۶ × line-travel ← انیمیشن خط‌چین SVG روی پس‌زمینه
  ۸ × pulse-dot   ← قرص‌های «LIVE»
  ۵ × lines-slide ← نوار متحرک روی هر اسلاید
  ۳ + ۳ + ۳       ← ستاره‌ها (هر لایه دو انیمیشن)، شهاب‌ها، هاله‌های محو
  … grid-drift, beam-scan, meteor ×۲, breathe, caret, ring-turn ×۲, sheen ×۳

FPS بی‌کار: ۱٫۱  (میانگین ۶۷۴ میلی‌ثانیه برای هر فریم)
```

با همان روش، وقتی فقط `.ambient` خاموش می‌شد نرخ فریم به ۳٫۵ می‌رسید و با خاموش‌کردن
همهٔ انیمیشن‌ها به ۱۴٫۵. یعنی سقفِ ممکن همین بود: هر چیزی که می‌جنبید، فریم را می‌خورد.

### ۱٫۲ سه علت مشخص

1. **بودجهٔ فریم موبایل.** ۴۵ انیمیشن هم‌زمان یعنی مرورگر پیش از تمام‌شدن فهرست، فریم را
   از دست می‌دهد؛ آن‌چه کاربر می‌بیند پرش و «چشمک» سطح صفحه است. در فارسی بدتر از
   انگلیسی بود به یک دلیل خسته‌کننده: متن فارسی بلندتر است، صفحه بلندتر است، پس
   لایه‌های بیشتری هم‌زمان زنده‌اند.
2. **`filter: blur(90px)` روی سه هالهٔ تمام‌صفحه.** هر فریم باید سه لایه را از کرنل ۹۰
   پیکسلی گاوسی رد کند. گرادیان شعاعی خودش یک محوِ نرم است؛ `filter` یک درجه نرمی
   می‌خرید و با کل بودجهٔ فریم پرداخت می‌کرد. حذف شد و گرادیان‌ها تا ۷۲٪ ادامه یافتند.
3. **`backdrop-filter` روی `.panel` و هدر چسبان.** یعنی مرورگر پشت هر کارت و پشت هدر را
   در هر فریمی که چیزی زیرش عوض شود، دوباره می‌خواند و محو می‌کند — در صفحه‌ای به
   ارتفاع ۲۳٬۰۰۰ پیکسل، یعنی هر فریم اسکرول. پنل‌ها روی پس‌زمینهٔ تقریباً مشکی، ۶۰ تا
   ۷۵ درصد کدر بودند؛ پس محو تقریباً هیچ چیزِ دیدنی نمی‌ساخت و تقریباً همه‌چیز را هزینه
   می‌کرد.

و یک «چشمک» واقعی، نه استعاره:

4. **کرسر تایپ‌رایتر با `steps(1)` خاموش‌روشن می‌شد** — یک هرتز، برای همیشه.
   `@keyframes caret { 50% { opacity: 0 } }` یک چشمکِ استاندارد است و چشم انسان برای
   دیدنش ساخته شده. حالا بین ۰٫۸۵ و ۰٫۲۵ نفس می‌کشد (۱٫۶ ثانیه، `ease-in-out`) و زیر
   `prefers-reduced-motion` می‌ایستد. این هم به همان گزارش می‌خورد و هم به WCAG 2.2.2.

### ۱٫۳ راه‌حل: بودجهٔ حرکت، بر اساس خودِ سند

در `scripts/landing-v2/styles.mjs` یک بخش «MOTION BUDGET» اضافه شد که با
`@media (hover: none), (max-width: 999px)` کار می‌کند — یعنی سند خودش می‌فهمد که
اشاره‌گر نمی‌تواند hover کند. **هیچ تشخیص user-agent وجود ندارد**؛ یک سند برای همه
سرو می‌شود (همان قاعده‌ای که در توضیح «cloaking» ژنراتور نوشته شده).

روی موبایل: پس‌زمینه می‌ماند ولی نمی‌جنبد (ستاره‌ها ثابت، شهاب و پرتو حذف، دو هالهٔ
بی‌حرکت و بدون blur)، زوم عکس‌ها می‌ایستد، نوارهای متحرک حذف می‌شوند،
`backdrop-filter`ها به پس‌زمینهٔ تقریباً کدر تبدیل می‌شوند، فلش‌های بازار و حلقه‌های کارت
نمی‌چرخند، و نقطه‌های «LIVE» آرام‌تر می‌شوند (۲٫۲ → ۵٫۲ ثانیه) تا نشانهٔ «فید زنده» بماند
بی‌آنکه هر ۲٫۲ ثانیه هفت بار نقاشی کند.

روی دسکتاپ (ماوس + viewport عریض) هیچ‌چیز کم نشد: آن نمایش برای همان‌جا طراحی شده بود.

### ۱٫۴ نتیجهٔ اندازه‌گیری‌شده

| | قبل | بعد |
|---|---|---|
| انیمیشن‌های بی‌نهایت (موبایل/فارسی) | ۴۵ | **۸** |
| FPS بی‌کار | ۱٫۱ (۶۷۴ms/فریم) | **۵۷٫۴** (۱۷٫۴ms/فریم) |
| بدترین فریم | ۸۳۳ms | **۵۰ms** |
| `filter: blur` روی لایه‌های تمام‌صفحه | ۳ | **۰** |
| `backdrop-filter` روی موبایل | ۱۱ قاعده | **۰** |
| CLS | ۰٫۰۳۶ | ۰٫۰۵۷ (یک رویداد، هنگام پر شدن داده) |

دو کار دیگر هم در همین مسیر:

- **پارالاکس اشاره‌گر روی لمسی خاموش شد.** `pointermove` روی گوشی در حین درگ/اسکرول هم
  شلیک می‌شود و هر بار دو custom property می‌نوشت — یک بازمحاسبهٔ استایل که سر همان
  فریم با اسکرول رقابت می‌کند. گوشی نمی‌تواند hover کند، پس چیزی برای شنیدن نبود.
- **خط توضیح کارت‌های بازار دو خط رزرو شد.** `«به‌روزرسانی»` به `«به‌روزرسانی ۲۱:۰۶»`
  تبدیل می‌شد و در ۳۹۰ پیکسل به خط دوم می‌رفت؛ ارتفاع کارت رشد می‌کرد و همهٔ پایین صفحه
  جابه‌جا می‌شد. یک عدد که زیر چشم خواننده جابه‌جا شود، همان شکایتِ «چشمک» است.

### ۱٫۵ یک دروغ کوچک که پیدا شد و برداشته شد

`markUpdated()` مهر `«به‌روزرسانی ۲۰:۵۹»` را **بی‌قید** می‌نوشت، حتی وقتی هر پنج فید
شکست خورده بودند و همهٔ اعداد صفحه «—» بودند. یعنی یک برچسب تازه روی دادهٔ ناموجود —
همان الگویی که موتور جست‌وجو و موتور پاسخ‌دهیِ هوش مصنوعی برای تشخیص صفحات ساختگی
می‌بیند. حالا مهر فقط وقتی نوشته می‌شود که فیدِ همان اعدادی که کنارشان نشسته
(`global` یا `markets`) واقعاً جواب داده باشد؛ وگرنه همان «داده در دسترس نیست» می‌ماند.

### ۱٫۶ اندازه‌گیری چیدمان

جددا اندازه‌گیری شد و هیچ عنصری در حالت ثابت ابعادش تغییر نمی‌کند (spread = 0 روی
`.dash`, `.pulse`, `#showcase`, `.hero` و…). یعنی «پرش» باقی‌مانده ساختاری نیست؛ همان
یک رویداد پر شدن داده است.

---

## ۲. سئو: شش صفحهٔ تازه، سه جفتِ دوزبانه

در `scripts/gen-landing.mjs` شش صفحهٔ تازه اضافه شد — برای همان سه قصدی که صفحه‌ای
نداشتند («سواپ ، کریپتو ، سرمایه گذاری»):

| فارسی | انگلیسی | هدف |
|---|---|---|
| `/سواپ-ارز-دیجیتال` | `/crypto-swap-without-kyc` | سواپ بدون ثبت‌نام و بدون احراز هویت |
| `/سرمایه-گذاری-در-ارز-دیجیتال` | `/crypto-investing-yield-and-lending` | سرمایه‌گذاری، بازده، وام‌دهی و ریسک‌ها |
| `/سواپ-سولانا` | `/solana-token-swap` | سواپ سولانا، SPL و Token-2022 |

**ترجمه، نه قالب.** هر صفحه برای خوانندهٔ خودش نوشته شده: فارسی برای کسی که دارد تصمیم
می‌گیرد به یک دامنهٔ `.ir` کیف پول وصل کند، انگلیسی برای کسی که اصلاً نمی‌داند این چیز
چیست. هر جفت با `hreflang` دوطرفه به هم وصل است (en + fa + x-default) — اگر یک صفحه
چیزی بگوید که همتای دیگرش نمی‌گوید، آن جفت یک باگ است.

**قاعدهٔ صداقت، سخت‌گیرانه‌تر از هرجای دیگر.** اینها اولین صفحه‌ای هستند که یک غریبه
می‌خواند، پس هیچ‌جا سود، قیمت، رتبه یا صرفه‌جویی وعده داده نمی‌شود. کارمزد همان ۰٫۷٪
موتور است از همان متغیری که خود برنامه می‌خواند، و هرگز بدون جملهٔ «پیش از امضا روی
همان صفحه دیده می‌شود» نمی‌آید. هر جا قابلیتی محدودیتی دارد، محدودیت در همان پاراگراف
نوشته شده: یادآورِ خرید پله‌ای معاملهٔ خودکار نیست، بازده یعنی پذیرش ریسک، و کسی نمی‌تواند
تراکنش را برگرداند.

### ۲٫۱ داده‌ساختار HowTo (برای موتورهای پاسخ‌دهی)

هر شش صفحه یک `HowTo` هم گرفتند. گوگل نتیجهٔ غنیِ HowTo را بازنشسته کرده، پس این برای
یک لینک آبی با آچار نیست؛ برای این است که وقتی کسی از یک دستیار می‌پرسد «چطور ارز
دیجیتال سواپ کنم؟»، فهرست مرتب گام‌ها بدون اجرای JavaScript قابل استخراج باشد.
دو قاعده ساختاری: همان گام‌ها **روی صفحه** به‌صورت فهرست مرتب رندر می‌شوند (مارک‌آپی
که چیزی را توصیف کند که صفحه نشان نمی‌دهد دقیقاً همان چیزی است که دستورالعمل داده‌های
ساختاری ممنوع می‌کند)، و متن در هر دو جا از یک منبع می‌آید تا هیچ‌وقت از هم جدا نشوند.

### ۲٫۲ فهرست شبکه‌ها: سه عدد متناقض در یک سایت

در همین بازبینی معلوم شد سایت سه ادعای مختلف داشت: عنوان `index.html` می‌گفت ۱۷ شبکه،
`og:description` می‌گفت ۱۴، توضیح JSON-LD می‌گفت «ده»، و صفحهٔ لندینگ دوزبانه در پنج جا
«۱۰ شبکه» می‌گفت.

عدد درست از خود رجیستری درآمد: `aggregatorSupports()` (کایبر، live-probed ۲۰۲۶-۰۹-۱۱/۱۳)
۱۳ زنجیرهٔ EVM، `openOceanSupports()` سه تای دیگر (Mantle، Scroll، zkSync Era) و سولانا
با مسیر خودش → **۱۷**. همه‌جا به همان یک عدد و یک فهرست یکسان شد (و شبکه‌ها در
صفحهٔ لندینگ از ۱۰ کارت به ۱۷ کارت رسیدند — هر نام شبکه خودش یک عبارت جست‌وجوشدنی است).

### ۲٫۳ برای موتورهای هوش مصنوعی

- **`public/llms.txt`** (تازه): توصیف کوتاه و واقعی برای مدل‌ها — امانت‌داری، کارمزد،
  همان ۱۷ شبکه، زبان‌ها، و بخش «چه چیزی نیست» (نه بانک، نه سود تضمینی، نه توصیهٔ مالی).
  هرچه در آن است، روی همان صفحه‌هایی که لینک می‌دهد هم هست.
- **`robots.txt`**: خزنده‌های هوش مصنوعی از قبل مجاز بودند (`User-agent: *`)؛ حالا
  صریح هم نام برده شده‌اند (GPTBot، OAI-SearchBot، ClaudeBot، PerplexityBot،
  Google-Extended و…). این هیچ رفتاری را عوض نمی‌کند و دقیقاً به همین دلیل امن است:
  آنچه عوض می‌شود تفسیر است — یک مجازِ کلی می‌تواند تصادفی خوانده شود، یک فهرست صریح
  یک بیانیهٔ قصد است.
- **`sameAs`** به Organization اضافه شد (تنها حساب عمومیِ واقعی پروژه) و `alternateName`
  فارسی به WebSite — این‌ها راهی هستند که یک موتور پاسخ، نام برند را به همان موجودیتی
  وصل کند که منظورش است.

### ۲٫۴ گراف لینک داخلی

هایپرلینک‌ها تنها چیزی هستند که یک خزندهٔ بدون JavaScript می‌تواند دنبال کند (مسیرهای
خود برنامه پشت `#` هستند). بلوک `<noscript>` صفحهٔ اصلی از ۴ راهنما به **۱۰** رسید و
هر صفحهٔ تولیدشدهٔ تازه اضافه شد. صفحه‌ای که تولید می‌شود، در sitemap می‌آید و به
IndexNow فرستاده می‌شود ولی با هیچ لینکی قابل‌دسترسی نیست، فقط شانس می‌آورد.

### ۲٫۵ sitemap و IndexNow

`sitemap.xml` از ۷ به **۱۴ نشانی** رسید (۱۳ صفحهٔ استاتیک + خود صفحهٔ اصلی) و
`scripts/submit-indexnow.mjs` همهٔ slugهای تازه را دارد — ممیزیِ `test/wiring.mjs` اگر
صفحه‌ای به ژنراتور اضافه شود و به IndexNow نرسد، قرمز می‌شود.

---

## ۳. اثر روی اندازهٔ بستهٔ اولیه

هیچ‌کدام از این تغییرات به `src/` دست نزد؛ باندل خودِ برنامه بایت‌به‌بایت همان است.
صفحهٔ لندینگ یک سند مستقل است (CSS و JS درون‌خط) و `llms.txt` یک فایل متنی است.

---

## ۴. تست‌ها

- `npm run build` سبز (۱۳ صفحهٔ لندینگ تولید شد).
- `npm run test:landing` → **۹۷ / ۰**.
- `node test/wiring.mjs` روی این شاخه: **۲۷۰۹ ادعا / ۱۵ خطا**؛ روی `main`:
  **۲۷۰۸ ادعا / ۱۵ خطا**. مجموعهٔ خطاها ماشینی مقایسه شد و **بدون هیچ تفاوت** است —
  در `main` چون `dist/` وجود نداشت، آزمونِ بودجهٔ باندل به‌کل اجرا نمی‌شد؛ با گذاشتن
  `dist/` روی همان درخت، همان ۱۵ خطا (شامل بودجهٔ ۱۵۹۲ کیلوبایتی) بازتولید شد. پس
  این تغییر هیچ خطای تازه‌ای اضافه نکرد.
- اندازه‌گیری چشمک/فریم/CLS که در بخش ۱ آمد، روی `dist/` واقعی و با موتور واقعی.

## ۵. کاری که مانده (عمداً)

- **CLS صفر نشد.** ۰٫۰۵۷ با یک رویداد، در باند «خوب» وب‌وایتیالز است. برای صفر شدنش
  باید ارتفاع ناحیه‌های پویا (جدول بازار و ردیف بازده هیرو) دقیقاً به اندازهٔ حالت
  اسکلتی رزرو شود؛ در عرض‌های مختلف اندازه‌ها فرق می‌کند و یک رزرو اشتباه از شیفت
  بدتر است. یک پچ جدا با اندازه‌گیری در سه عرض لازم دارد.
- **ادعای «۱۰ شبکه» در `scripts/landing-v2/copy.mjs`** اصلاح شد، ولی فهرست اسلایدها و
  متن‌های دیگر ممکن است همان عدد قدیمی را در جای دیگری تکرار کنند؛ یک `grep` روی
  «۱۰ شبکه / ten networks» پیش از انتشار بعدی ارزش دارد.
- **Google Search Console** قابل اسکریپت‌شدن نیست: `sitemap.xml` خودش خوانده می‌شود و
  IndexNow بی‌نیاز از حساب است، ولی برای گزارش پوشش و کوئری‌های واقعی باید یک‌بار
  دستی وارد شد.

---

## ۲٫۶ وبلاگ: شش راهنمای بلند و دو هاب

سه جفتِ راهنمای بلند اضافه شد، هر کدام یک استدلال کامل در زبان خودش:

| فارسی | انگلیسی | پرسشی که جواب می‌دهد |
|---|---|---|
| `/کارمزد-سواپ-ارز-دیجیتال` | `/how-crypto-swap-fees-work` | کارمزد سواپ از چه چیزی ساخته شده و پیش از امضا کجا باید دید |
| `/تفاوت-کیف-پول-امانی-و-غیرامانی` | `/custodial-vs-non-custodial-wallets` | با نگه‌داشتن کلید چه چیزی از دست می‌دهی |
| `/بدون-احراز-هویت-چه-چیزی-خصوصی-می‌ماند` | `/what-stays-private-without-kyc` | «بدون احراز هویت» چه چیزی را می‌خرد و چه چیزی را نه |

هر راهنما با `BlogPosting` علامت خورده (تاریخ واقعی، ناشر، مجموعه) و دو هاب `/blog` و
`/وبلاگ` با `Blog`. نکتهٔ ساختاری: فهرست هاب از خودِ آرایهٔ `POSTS` رندر می‌شود، پس
راهنمایی که تولید شود و روی فهرست لینک نشود وجود ندارد — همان «صفحهٔ یتیم» که فقط با
خزیدن مستقیم پیدا می‌شود.

نویسنده **شرکت** است و نه یک اسم ساختگی. یک بای‌لاین جعلی چیزی است که یک بازبین با یک
کلیک باطل می‌کند و اعتبار کل گراف داده‌ساختاری را با خودش می‌برد.

### ۲٫۷ یک باگ واقعی در همین مسیر

پیش‌فرضِ متن هشدار ریسک و متن دکمه در تابع رندر، **رشتهٔ انگلیسی هاردکد** بود. هر صفحهٔ
فارسی که متن ریسک خودش را ننوشته بود، یک پاراگراف انگلیسی وسط صفحهٔ فارسی تحویل
می‌گرفت — و تا امروز هیچ صفحه‌ای آن را ندیده بود چون هر شش صفحهٔ فارسی قبلی متن خودشان
را داشتند. حالا پیش‌فرض‌ها در `copyFor()` و به زبان خود صفحه‌اند و `qa-dist` همین را
تست می‌کند: «راهنمای فارسی، پاراگراف انگلیسی ندارد».

### ۲٫۸ بودجهٔ حرکت، این بار روی قالب خودِ راهنماها

لندینگ دوزبانه با موتور واقعی اندازه‌گیری شده بود، ولی قالب راهنماها (`gen-landing`)
همان الگوی قدیمی را داشت: گریدِ متحرک، سه هالهٔ شناور با `filter: blur(24px)`، حلقهٔ
چرخان، نوار براق و نقطهٔ تپنده — همه بی‌نهایت. همان قاعده این‌جا هم اعمال شد: زیر
`@media (hover: none), (max-width: 999px)` همه روی فریم اول می‌ایستند، محوِ ۲۴ پیکسلی
برداشته می‌شود (گرادیان شعاعی خودش لبهٔ نرم دارد) و `backdrop-filter` پنل‌ها حذف
می‌شود. انیمیشن‌های کوتاه و متناهی (ظهور ۰٫۷۵ ثانیه‌ای، حالت reduced-motion) دست
نخوردند.

> صادقانه: این تغییر روی قالب راهنماها **دوباره اندازه‌گیری نشد** — همان درمانِ
> اندازه‌گیری‌شدهٔ لندینگ است که روی همان الگو اعمال شده. برای اندازه‌گیری تازه به یک
> مرورگر واقعی نیاز بود و دانلود باینری مرورگر در این محیط بسته است (هم
> `googlechromelabs.github.io` و هم `deb.debian.org` پاسخ ندادند).
