# سهام/فروشگاه — حذف تب‌های بنر فلیپ، قانون زبان، و «چطور کار می‌کند» به زبان کاربر (۲۰۲۶-۰۹-۲۴)

سه گزارش در یک پیام:

> «در صفحه سهام تب توکیتزه، بالای بنر فلیپ کارت دو تب هست بنام RWA و افق جهانی.
> تب را حذف کن و داخل بنر بالا سمت چپ ایکون تعویض بزار بدون عنوان و اتومات هر ۴۰
> ثانیه خودش برگردد فلیپ کارت.»
>
> «وقتی روی زبانی به غیر فارسی و انگلیسی باشد فلیپ کارت فارسی هست در صورتی که
> باید انگلیسی باشد.»
>
> «داخل فروشگاه وقتی روی کارتی میزنی و چطور کار می‌کند حتی وقتی زبان مثلا فارسی
> باشد باز هم انگلیسی هست درست کن.»

---

## ۱ · بنر فلیپ: تب‌ها حذف، یک ایکون تعویض، هر ۴۰ ثانیه

`src/components/RwaHorizonFlipBanner.jsx` + `src/styles/rwa-flip-banner.css`

| قبل | بعد |
|-----|-----|
| دو تب `RWA` / `افق جهانی` **بالای** کارت، با یک پیلِ لغزان و نوار پیشرفت | یک دکمهٔ **فقط-آیکون**، **بدون عنوان**، داخل بنر (`<IconSwap />` — دو فلش که جای هم را می‌گیرند) |
| `AUTO_MS = 12000` | `AUTO_MS = 40000` — «هر ۴۰ ثانیه خودش برگردد» |
| نوار پیشرفت زیر تب | حلقهٔ شمارش معکوس **دور همان آیکون**؛ `animationDuration` از همان `AUTO_MS` می‌آید، پس شمارش و چرخش نمی‌توانند از هم جدا شوند |

دکمه **بیرون از استیجِ 3D** قرار گرفته و روی گوشهٔ کارت می‌نشیند. داخلِ
`preserve-3d`، اینکه دکمه روی کارت نقاشی شود یا از پشتش بیرون بزند به
hit-testing سه‌بعدی مرورگر بند است — همان دسته‌ای که قبلاً لرزش اندروید را
ساخت. اینجا یک دکمهٔ معمولیِ absolute است: همیشه بالا، همیشه قابل لمس.

**جای دکمه.** `inset-inline-end` است، نه `left`. در چیدمان RTL — همان که
مالک می‌بیند و «بالا سمت چپ» را برای آن گفته — inline-end یعنی **چپ فیزیکی**،
پس دکمه در گوشهٔ بالا-چپ می‌نشیند. در LTR همان اعلان دکمه را بالا-راست
می‌گذارد، که گوشهٔ خالی است: آیکون ۶۲ پیکسلیِ هر فیسِ LTR مالکِ بالا-چپ است و
دکمه‌ای روی آن شکسته به نظر می‌رسد. یک اعلان، گوشهٔ درست در هر دو جهت، بدون
هم‌پوشانی — در `test/stocks-flip-banner-probe.mjs` هم قفل شده.

**یک باگ که سر راه پیدا شد و بسته شد:** توقفِ خودکار روی hover با
`matchMedia('(hover: hover)')` گارد شده. یک تپِ لمسی یک `mouseenter`
مصنوعی می‌فرستد که هیچ‌وقت `mouseleave` دنبالش نمی‌آید؛ یعنی روی گوشی اولین
لمس، چرخش خودکار را تا ابد خاموش می‌کرد — دقیقاً همان قابلیتی که این تغییر
می‌خواهد راه بیندازد.

## ۲ · زبان بنر: فارسی فقط برای فارسی

```js
// قبل — زبان را از RTL بودنِ چیدمان حدس می‌زد
const isEn = l.startsWith('en') || (!isRTL && !l.startsWith('fa'));
// بعد — همان قانونی که گفته شد
const isEn = !l.startsWith('fa');
```

با زبان عربی یا اردو (RTL، ولی نه فارسی) کارت متن **فارسی** نشان می‌داد.
`isRTL` می‌گوید متن به کدام طرف می‌رود، نه این‌که به چه زبانی است. حالا فقط
`fa` فارسی می‌گیرد و بقیهٔ زبان‌ها انگلیسی — همان fallbackی که i18next در کل
اپ دارد (رشتهٔ ترجمه‌نشده انگلیسی دیده می‌شود، نه کلید خام). تست
`test/smart-wallet-session-and-flip-banner.test.jsx` روی ar/ur/tr/ru/en
قفلش می‌کند و تأیید می‌کند در فیسِ اول هیچ حرف فارسی نمانده.

## ۳ · فروشگاه: «چطور استفاده کنم» به زبان کاربر

**ریشه، یک رشتهٔ ثابت بود:** `&lang=en` در `server/shop.js`. خودِ API
Cryptorefills بومی‌سازی دارد — `rich_description.locale` هر زبانی که بخواهی
برمی‌گردد، وقتی داشته باشد:

| درخواست | پاسخ واقعی (زنده چک شد) |
|---------|--------------------------|
| `lang=es` | `locale: "es"` — متن و مراحل اسپانیایی |
| `lang=ar` | `locale: "ar"` — متن و مراحل عربی |
| `lang=fa` | `rich_description: null` |

`null` خطرناک است، نه خطا: **مراحل استفاده، راهنما و هشدارِ قفلِ منطقه‌ای
با هم ناپدید می‌شوند.** اگر زبان کاربر را بی‌گارد فوروارد می‌کردیم، هشدارِ
انگلیسیِ کاربر فارسی‌زبان با **هیچ** عوض می‌شد — بدتر از باگِ گزارش‌شده.

پس: به زبان کاربر می‌پرسیم، اگر پاسخشان نثر نداشت **یک بار دیگر به انگلیسی
می‌پرسیم** و آن زبان را برای همان نمونهٔ سرور به‌خاطر می‌سپاریم
(`NO_RICH_CONTENT`)، و `contentLocale` می‌گوید *واقعاً* چه زبانی برگشت — نه
زبانی که خواستیم. سرورهای بعدی همان زبان را مستقیم انگلیسی می‌خوانند، پس
کاربر فارسی‌زبان یک بار هزینهٔ probe را می‌دهد.

سمت UI (`src/pages/Shop.jsx`):

- پروفایلِ درخواست‌شده زبان کاربر است (`uiLang`) و با هر تغییر زبان کارت
  دوباره خوانده می‌شود.
- اگر متنِ برگشته در زبانِ کاربر بود → همان متن، بی‌هیچ تغییر (رفتار قبلی).
- اگر نبود → جعبهٔ «چطور استفاده کنم» با **چک‌لیست فارسیِ خودمان** باز می‌شود
  (۵ گام: مبلغ، تحویل به کریپتوریفیلز، پرداخت با کریپتو، کد با ایمیل، محل
  استفاده) و متن اصلیِ ارائه‌دهنده **پایین‌تر، با برچسب زبانش** می‌آید.
- یادداشتِ قفلِ منطقه‌ای (هشدارِ پول‌سوز) در همهٔ حالت‌ها می‌ماند و وقتی
  انگلیسی است با `dir="ltr"` رندر می‌شود تا الگوریتم bidi نقطه‌گذاری لاتین را
  جابه‌جا نکند.
- هیچ‌جا `dangerouslySetInnerHTML` نیست و هیچ ترجمهٔ ماشینی‌ای انجام نمی‌شود.

کلیدهای ترجمه در `fa` و `en` (`shop.providerNote`, `shop.providerHowTo`,
`shop.redeemSteps.s1..s5`). برای زبان‌هایی که ترجمه ندارند، گام‌های ما
**انگلیسی** نمایش داده می‌شود و متنِ ارائه‌دهنده هم کنارش می‌ماند — نه
دوباره‌کاری بی‌فایده، و نه ادعای ترجمه‌ای که وجود ندارد
(`ownStepsTranslated` این را با مقایسهٔ رشتهٔ رندرشده با `en.json` می‌سنجد).

---

## تست‌ها

| چه چیزی | کجا |
|---------|-----|
| ۵۰ بررسی چیدمان/تم/ساختار بنر، بدون تب، با حلقهٔ ۴۰ ثانیه‌ای و قانون `fa` | `test/stocks-flip-banner-probe.mjs` |
| چرخش با دکمه، ۴۰ ثانیه، بازگشت خودش، نبود تب، زبانِ ar/ur/tr/ru/en | `test/smart-wallet-session-and-flip-banner.test.jsx` |
| probeِ زبانِ سرور: es یک تماس، fa دو تماس، بار دوم یک تماس، زبان بدشکل هرگز در URL | `test/shop-revenue-probe.mjs` |
| شیتِ فروشگاه، واقعاً mount شده: گام‌های فارسی، متن اصلی برچسب‌دار، هشدار گم نشده | `test/shop-howto-language.test.jsx` (از `npm run test:shop-revenue`) |
| ۲۷۰۹ بررسی static wiring | `test/wiring.mjs` |

`npm test` در این سندباکس روی `test/etf-gold-provider-probe.mjs` می‌افتد
(`GOLD_SPOT_SHAPE_UNUSABLE`)؛ روی **کامیتِ پایهٔ main هم همان‌جا و با همان
پیام می‌افتد** — یعنی از این تغییر نیست (بدون شبکه بودنِ سندباکس). سه شکستِ
دیگرِ `test/screens.jsx` (Welcome و Farm) هم عیناً روی main وجود دارند.

## کاری که انجام نشده

- متنِ خودِ بنر فلیپ برای ۱۲ زبان ترجمه نشده؛ فقط `fa` فارسی است و بقیه
  انگلیسی — همان قانونی که خواسته شد.
- زبان‌هایی که Cryptorefills پوشش نمی‌دهد (مثلاً `fa`) متنِ اصلی‌شان انگلیسی
  می‌مانَد؛ ما فقط **جلوترِ** آن یک راهنمای بومی گذاشتیم، حذفش نکردیم.
