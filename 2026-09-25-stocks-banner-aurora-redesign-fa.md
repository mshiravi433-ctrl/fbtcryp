# سهام → تب «سهام توکنیزه» — بنر RWA / افق جهانی: «دکمه چپ و راست» حذف، باکس شیشه‌ایِ متحرک، بزرگ‌تر (۲۰۲۶-۰۹-۲۵)

درخواست، عیناً:

> «در صفحه سهام تب توکنیزه بنر RWA و افق جهانی — دکمه چپ و راست را در بنر
> مخفی کن — تم مناسب و زبان مناسب — بنر باید ایکون مدرن و انیمیشن بیشتر و
> باکس بسیار مدرن‌تر و خاص‌تر شود، با دقت یه چیز بی‌نظیر درست کن — اندازه
> هم می‌تونی کمی بزرگ‌تر شود»

---

## ۱ · «دکمه چپ و راست» یک باگ بود، نه یک قابلیت

بنر قبلی دو نقطهٔ ۵ پیکسلیِ نشانگر داشت که به‌صورت `<button>` رندر می‌شد.
قانونِ سراسریِ حداقلِ ناحیهٔ لمس در `src/index.css`:

```css
button, [role="button"], a.btn, .tap-target {
  min-height: var(--min-tap);   /* 44px */
  min-width: var(--min-tap);
}
```

هر نقطه را به یک **قرصِ ۴۴ پیکسلیِ خاکستری** تبدیل می‌کرد — دو دایرهٔ بزرگ
وسطِ بنر، روی زیرعنوان، یکی چپ و یکی راستِ مرکز. با یک Chromium واقعی روی
همان استایل‌شیتِ اپ رندر و تأیید شد (تم روشن و تاریک، فارسی و انگلیسی).

| قبل | بعد |
|-----|-----|
| دو `<button>` ۵px که قانونِ ۴۴px آن‌ها را باد می‌کرد | دو **قطعهٔ باریک** (`<span>`، `pointer-events: none`) — هیچ قانونِ دکمه‌ای به آن نمی‌رسد |
| دکمهٔ گردِ فلش در انتهای هر اسلاید | حذف؛ کلِ اسلاید از قبل هدفِ لمس بود، فلش یک دکمهٔ تکراری بود که شبیه کنترلِ چرخ‌وفلک می‌خواند |
| — | قطعهٔ فعال در همان ۱۰ ثانیهٔ توقف **پر می‌شود**؛ بدون هیچ کنترلی می‌گوید «این خودش می‌چرخد» |

تنها `<button>`های باقی‌مانده خودِ دو اسلایدند. کاربر کیبورد با کلیدهای جهت
جابه‌جا می‌شود (در RTL کلیدِ چپ یعنی بعدی)، کاربر لمسی سوایپ می‌کند، بقیه ده
ثانیه صبر می‌کنند. پرشدنِ قطعه با هر تعویضِ اسلاید **و** با هر توقف/ادامه
دوباره کلید می‌خورد (`key`)، پس پرشدن و تایمر هرگز از هم جدا نمی‌افتند.

## ۲ · باکس

`src/components/RwaHorizonSwapBanners.jsx` + `src/styles/rwa-banners.css`
(بخش `.rhs`؛ بنرِ سرصفحهٔ تب RWA — `.rwb` — دست نخورده).

- **یک سطح شیشه‌ای از توکن‌های خودِ اپ** (`--bg-panel`، `--line`، `--text-…`)،
  زیرش یک هالهٔ رنگیِ ملایم به رنگِ اسلاید.
- **دو میدانِ شفق (aurora)** — طلایی/بنفش برای RWA، فیروزه‌ای/سبز برای افق —
  که آهسته می‌لغزند و با تعویضِ اسلاید در هم محو می‌شوند؛ کلِ باکس با محتوا
  عوض‌حال می‌شود.
- **دنباله‌دارِ نور** روی خطِ دورِ کادر (یک `conic-gradient` که با `transform`
  می‌چرخد — نه انیمیشنِ `--angle` که هر فریم نقاشی می‌خواهد)، **ورقِ نور** که
  هر ۷٫۵ ثانیه از روی شیشه رد می‌شود، **شبکهٔ نقطه‌ایِ کم‌رنگ** برای عمق.
- **آیکونِ «گوی» ۶۰px**: دو حلقهٔ خلاف‌جهت (خط‌چین + کمان) دورِ یک هستهٔ
  شیشه‌ای که گلیفِ ترسیم‌شده را نگه می‌دارد — شمش‌ها با برقِ سطح، توکنِ در
  مدار و جرقه؛ کرهٔ زمین با نصف‌النهارِ چرخان، خطِ روندِ خودکش و ماهواره.
- **اسپارک‌لاینِ خودکش** در نیمهٔ انتهاییِ هر اسلاید، با ماسک تا هرگز با
  عنوان رقابت نکند. نمودار در هر دو جهت چپ‌به‌راست می‌ماند (نموداری که به چپ
  بالا برود، برای هر چشمِ نمودارخوانی «ریزش» است).
- **اندازه:** ۸۶ → **۱۱۲px** روی گوشی، ۹۶ → **۱۲۸px** از ۶۴۰px؛ گردیِ گوشه
  ۱۸/۲۰ → ۲۲/۲۴. متن همان دو خطِ قبلی است (عنوان + یک خط)، فقط جداکننده‌ها
  به‌جای «·» تایپ‌شده، نقطه‌های رنگیِ ترسیم‌شده‌اند. روی ۳۲۰px بلندترین خط
  («Forex · Gold · Stocks · Indices») با تنگ‌ترکردنِ جداکننده‌ها جا می‌شود،
  نه با حذفِ کلمه.

### بودجهٔ حرکت

همه‌چیز با `transform`/`opacity` حرکت می‌کند (کارِ کامپوزیتور)، جز دو
خط‌کشیِ SVG با `stroke-dashoffset`. اسلایدی که دیده نمی‌شود
`animation-play-state: paused` دارد. زنده روی گوشی: ۱۵ لایهٔ ارزان روی
**یک** بنر؛ در `prefers-reduced-motion` **صفر** انیمیشنِ در حال اجرا و یک
فریمِ ثابتِ کامل (خط کشیده‌شده، پُرشدگی پیدا، دنباله‌دار و ورقِ نور خاموش)
— با Chromium شمرده شد.

### تم

- تاریک: شفق ۲۶٪/۲۰٪، دنباله‌دار روشن، ورقِ نور سفیدِ ۷٫۵٪.
- روشن: اکسنت‌ها عمیق‌تر (`#a86a00`، `#00758a`، بنفش `#5b46c9`، سبز
  `#00795c`)، شفق نصف (۱۴٪/۱۰٪)، دنباله‌دار کم‌رنگ‌تر، ورقِ نور سفیدِ
  ۸۵٪ — یک ورقِ تیره روی شیشهٔ سفید شبیه سایه می‌شود. بلوکِ
  `prefers-color-scheme: light` هم همین مقادیر را دارد تا پیش از ست‌شدنِ
  `data-theme` هم درست باشد.
- کنتراست، روی سطحِ ترکیب‌شده (پنل + شفقِ زیرِ متن، هر دو پالت)، در سه حالتِ
  تاریک / روشن / روشنِ پیش‌از‌نقاشی: عنوان ≥ ۱۴٫۶:۱، زیرعنوان ≥ ۶٫۳:۱.

### زبان

همان قانونِ پیشینِ مالک: فارسی **فقط** برای `fa`، انگلیسی برای بقیه؛ چیدمان
از `dir` صفحه پیروی می‌کند (جای اسپارک‌لاین با `inset-inline-end`، مبدأ
پرشدنِ قطعه در RTL از راست، پارالاکسِ متن با علامتِ `--dir`).

## یک باگ که سرِ راه پیدا و بسته شد

مسیرِ اسپارک‌لاین `fill="none"` نداشت. پیش‌فرضِ SVG پُرکردنِ سیاه است و یک
مسیرِ باز با وتری از نقطهٔ آخر به اول بسته می‌شود — نتیجه یک **گوهٔ سیاه**
زیرِ خط بود که در تم روشن مثل سایه دیده می‌شد. حالا `fill="none"` است و
probe و تستِ mount هر دو همین را قفل می‌کنند.

## تست‌ها

| چه چیزی | کجا |
|---------|-----|
| ۶۱ بررسی: بدون 3D، سواپِ افقی و علامتِ RTL، **فقط یک نوع `<button>`** (اسلاید)، نشانگرِ غیرِدکمه و بدون pointer، فلشِ «go» حذف، اندازه‌های تازه، ۱۶ `@keyframes` فقط transform/opacity/stroke-dashoffset، توقفِ اسلایدِ پنهان، reduced-motion، کنتراست WCAG در سه تم با شفقِ زیرِ متن، پالتِ اسلاید روی ریشه، قانونِ `fa` | `test/stocks-swap-banner-probe.mjs` (از `test/run.mjs`) |
| mount واقعی: فقط دو دکمه و هر دو اسلایدند، نشانگر `aria-hidden` و بدون دکمه، دکوراسیون و گلیف‌ها حاضرند، گرادیان‌ها id تکراری ندارند، کلیدهای جهت (RTL و LTR)، ۱۰ ثانیه و بازگشت، تپ توقف نمی‌کند، سوایپِ RTL، زبانِ ar/ur/tr/ru/en با `dir` درست | `test/smart-wallet-session-and-flip-banner.test.jsx` (۱۲ تست) |
| ۲۷۰۸ بررسیِ static wiring — همان ۱۴ شکستِ پیشینِ `main`، هیچ‌کدام از این تغییر نیست | `test/wiring.mjs` |
| `vite build` سبز؛ CSS در `dist/assets/Stocks-*.css` با `color-mix`/`conic-gradient` دست‌نخورده | — |

## کاری که انجام نشده

- بنرِ سرصفحهٔ تب RWA (`RwaSectionBanner`، `.rwb`) همان‌طور که بود مانده؛
  درخواست فقط بنرِ تب «سهام توکنیزه» بود.
- متنِ بنر برای ۱۲ زبان ترجمه نشده؛ همان قانونِ «فارسی فقط برای فارسی».
