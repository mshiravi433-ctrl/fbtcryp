# ۲۰۲۶-۰۹-۲۴ (۴) — چهار تغییرِ رابط: پاپ‌آپِ شبکه در والت، حذف آیکون تلفن از پل سولانا، هشدارِ پروکسی بعد از انتخاب فارسی، آهنگ اختصاصی نوتیفیکیشن

چهار گزارش، یک بند. هر چهارتا در ظاهر کوچک‌اند و هر چهارتا در واقع «آیا این برنامه قابل اعتماد است یا نه»
را تعیین می‌کنند: یکی انتخاب را از یک باکسِ بریده به یک پنجره تبدیل می‌کند، یکی یک وعدهٔ دروغ (رفتن به
گوشی) را برمی‌دارد، یکی قبل از اولین شکست می‌گوید چرا شکست رخ می‌دهد، و یکی صدای خودِ برنامه را به
نوتیفیکیشن برمی‌گرداند.

---

## ۱. انتخاب شبکه در صفحهٔ والت: پاپ‌آپ شد

**گزارش:** «در صفحه والت اصلی که نوشته شبکه و باید از لیست بازشونده شبکه انتخاب کنی باید به صورت پاپ اپ
شود وقتی روش میزنی و شبکه را انتخاب کنی.»

### چه بود

`NetworkSelect` یک **popover اینلاین** بود: یک `<div>` با `position: absolute` درست زیرِ دکمه، داخل همان
کارتِ hero. دو مشکل داشت که روی دسکتاپ پنهان می‌ماند و روی گوشی خودش را نشان می‌داد:

1. کارتِ hero والت `overflow: hidden` دارد — لیستِ شانزده‌تایی از لبهٔ کارت بیرون می‌زد و **بریده می‌شد**؛
2. هر صفحه‌ای داخل `PageTransition` است که روی `<motion.main>` یک `transform` می‌نویسد، و طبق spec یک
   عنصرِ transform‌شده **containing block** فرزندانِ `position: fixed` می‌شود. همان باگِ کلاسیکی که پاپ‌آپ
   تنظیماتِ سواپ را زیرِ fold می‌برد (توضیحش در `src/components/Sheet.jsx` هست).

### چه شد

لیست حالا از طریق `createPortal` به `document.body` می‌رود:

- **پس‌زمینهٔ تارِ کم‌نور** (`.net-select__backdrop`) که تپ روی آن فقط می‌بندد، هرگز انتخاب نمی‌کند؛
- **یک کارتِ مستقل** که روی گوشی از پایین بالا می‌آید (دسترسیِ شست، و جهتِ طبیعیِ «آمدن») و روی صفحهٔ
  عریض (≥۷۲۰px) وسط می‌نشیند؛
- **هدر با عنوان و دکمهٔ بستنِ ۳۴px** — پنجره‌ای که فقط با حدس‌زدنِ بیرونش بسته شود، روی گوشی تله است؛
- **`lockBodyScroll`** هنگام باز بودن: بدون آن، سوایپ روی پس‌زمینه صفحهٔ والت را می‌کشید و پاپ‌آپ به یک
  پس‌زمینهٔ متحرک می‌چسبید؛
- `z-index` روی ۲۱۰/۲۱۱، بالاتر از شیت‌های برنامه (۲۰۰/۲۰۱) چون این پاپ‌آپ می‌تواند از داخل یک شیت باز شود.

رفتارهای قبلی دست‌نخورده ماندند و در تست قفل شدند: جهت‌نماها، Home/End، Escape و تپِ بیرون **بدون انتخاب**
می‌بندند، جعبهٔ جستجو هنگام باز شدن فوکوس می‌گیرد، و `aria-activedescendant` ردیفِ هایلایت را اعلام می‌کند.

> نکتهٔ پیاده‌سازی که سزاوارِ ثبت است: پاپ‌آپ فرزندِ `document.body` است، نه فرزندِ `rootRef`. یک چکِ
> سادهٔ «آیا داخل رفرنس من است؟» هر تپ روی لیست را «بیرون» می‌دید و پنجره را قبل از اینکه کلیکِ انتخاب
> اصلاً فرود بیاید می‌بست. برای همین `popRef` هم در همان چک شریک است.

**فایل‌ها:** `src/components/NetworkSelect.jsx`، `src/styles/network-select.css`، `src/pages/Wallet.jsx`،
`test/network-select.test.jsx` (۵ تستِ تازه: پورتال، پس‌زمینه، تپِ داخل، قفلِ اسکرول، هنرهای آفلاین).

---

## ۲. آیکون تلفن از دکمهٔ «اتصال کیف پول سولانا» رفت

**گزارش:** «در بخش پل سولانا دکمه اتصال کیف مول سولنا یک ایکون تلفن داره اوت را ببر.»

`IconPhone` کنارِ این دکمه درست برعکسِ واقعیت حرف می‌زد: می‌گفت «ادامه‌اش روی گوشی شماست»، در حالی که
این دکمه **صفحه را ترک نمی‌کند** — به تبِ سولانای والت (`/wallet?tab=solana`) می‌رود و شیتِ اتصال همان‌جا،
روی هر اندازه صفحه‌ای (دسکتاپ هم)، باز می‌شود. یک گوشیِ کوچک کنارِ «اتصال کیف پول سولانا» وعدهٔ تحویل به
دستگاهِ دیگری می‌داد که هرگز اتفاق نمی‌افتد؛ و وعده‌ای که عملی نشود، «دکمه خراب است» خوانده می‌شود، نه
«قدم بعدی».

آیکون حذف شد و برچسبِ متنی — که همان دستورِ درست است — تنها ماند. `IconPhone` در `Icons.jsx` باقی است
(یک مجموعهٔ آیکونِ مشترک) و دیگر هیچ مصرف‌کننده‌ای ندارد.

**فایل:** `src/components/SolanaBridgePanel.jsx`.

---

## ۳. هشدارِ مدرنِ پروکسی، بلافاصله بعد از انتخابِ فارسی

**گزارش:** «در صفحه ولکام پس انتخاب زبان فارسی یک پاپ هشدار مدرن بیاد که بگه چون خیلی امکانات ما بر روی
بستر خارج هست بهتر است از [پروکسی] استفاده کنید، مدرن باشد با اندازه و فاصله دریت و جمله بندی درست.»

### چرا یک پاپ‌آپ و نه یک خط متن

یک جمله زیرِ لیست زبان‌ها را هیچ‌کس نمی‌خواند — آنجا کنارِ تنها کنترلی است که کاربر برایش آمده، یعنی
chrome محسوب می‌شود. و این **تنها** توصیه‌ای است که تعیین می‌کند برنامه روی یک اتصالِ فیلترشده اصلاً کار
می‌کند یا نه. پس: یک مدال، یک پیام، یک دکمه.

### چرا «فیلترشکن (پروکسی یا VPN)» و نه فقط «VPN»

در این بازار همهٔ کلمهٔ «فیلترشکن» را می‌شناسند؛ کسی که هرگز پروکسی پیکربندی نکرده هم می‌داند با آن چه
کند. فقط نامِ خارجی‌اش را گفتن یعنی توصیه به زبانِ اصطلاح. هر دو در متن هست.

### چرا فقط فارسی

این توصیه برای بازاری است که به‌عنوان یک گروه، از پشت شبکهٔ فیلترشده وصل می‌شود. پاپ‌آپی که همه باید از
آن رد شوند، برای بقیه فقط یک کلیکِ اجباری است. زبان انتخاب‌شده به قوت خودش باقی می‌ماند و بستنِ پنجره
هیچ چیزی را در جریان عوض نمی‌کند.

### جمله‌بندی (فارسی)

> **یک نکتهٔ مهم پیش از شروع**
>
> بیشتر امکانات این برنامه — قیمت‌های زنده، سواپ، پل بین‌شبکه‌ای و دستیار هوشمند — روی سرورهای خارج از
> کشور اجرا می‌شود. برای اینکه همه‌چیز کامل و سریع باز شود، بهتر است پیش از ادامه فیلترشکن (پروکسی یا
> VPN) خود را روشن کنید.
>
> _اگر فیلترشکن روشن نباشد، بعضی بخش‌ها کند یا ناتمام باز می‌شوند. این محدودیت شبکه است، نه مشکل برنامه
> — و در هیچ حالتی دارایی شما را به خطر نمی‌اندازد._

پاراگرافِ دوم عمداً دربارهٔ **پیامدِ نادیده گرفتن** است، نه تکرارِ هشدار: کندی به شبکه نسبت داده می‌شود،
نه به برنامه، و صریحاً می‌گوید دارایی در خطر نیست — چون در یک برنامهٔ مالی، یک هشدارِ مبهم یعنی وحشت.

### اندازه و فاصله

- **حداکثر ۴۰۰px عرض**: هشداری که عرضِ مانیتور را بگیرد ناخوانا است؛ دو سه خطِ کوتاه در هر پاراگراف اصل
  مطلب است.
- **۲۶/۲۲/۲۲px پدینگ و ۱۴px فاصلهٔ بینِ بلوک‌ها**: آیکون، عنوان، دو پاراگراف و دکمه یک **شیء** با هوای
  دورش خوانده می‌شوند، نه چهار چیزِ چسبیده به هم.
- **`line-height: ۱.۹`** برای بدنه: فارسی به بیشتر از لاتین لیدینگ نیاز دارد — خطِ زمینه‌اش بالا می‌نشیند
  و نقطه‌های «ی» و «ژ» آویزان می‌مانند؛ با لیدینگِ لاتین، نقطه‌ها به خطِ بالایی می‌چسبند.
- همه‌چیز با **ویژگی‌های منطقی** (margin-inline، padding-inline، text-align: start روی یادداشت) تا در RTL
  آینه باشد، نه شکسته.
- آیکون با **رنگِ کهربایی** (`--rgb-5`)، رنگِ هشدارِ برنامه، نه فیروزه‌ایِ برند: این تنها جای صفحهٔ خوش‌آمد
  است که جشن نیست، و رنگ سریع‌ترین راهِ گفتنِ این است قبل از اینکه کلمه‌ای خوانده شود.
- تمِ روشن: پس‌زمینهٔ سفیدِ یک‌دست، سایهٔ ملایم، و اسکریمِ خنثی — اسکریمِ نزدیک‌به‌مشکیِ تمِ تاریک روی
  صفحهٔ روشن کلِ صفحه را خاکستری می‌کند و پنجره شبیه حالتِ خطا می‌شود.

**فایل‌ها:** `src/components/ProxyAdvicePopup.jsx` (تازه)، `src/styles/proxy-advice.css` (تازه)،
`src/pages/Welcome.jsx`، کلیدهای `welcome.proxy.*` در `en/fa/ar`، `test/proxy-advice.test.jsx` (۹ تست:
فقط فارسی، متنِ واقعی، پورتال، `aria-labelledby/describedby`، Escape، و اینکه **گیت نیست** — Continue
در حالی که پنجره باز است کار می‌کند).

---

## ۴. آهنگ اختصاصی نوتیفیکیشن اندروید

**گزارش:** «نوتیفیکیشن اپلیکیشن وقتی میاد روی صفحه اندروید که از بالا به پایین میاد یک اهنگ نوتیفیکیشن
بزنه که خاص باشد.»

### صدا

`android/app/src/main/res/raw/fbt_notification.wav` — ۱.۳۵ ثانیه، مونو، ۴۴.۱kHz، ۱۶-بیت PCM، ۱۱۶ کیلوبایت.
سه ضربهٔ بالارونده از آکوردِ لا ماژور (A5 → E6 → A6)، هر کدام یک دستهٔ کوچک هارمونیک با حملهٔ سریع و
افتِ نمایی — شکلِ یک زنگِ زده‌شده، نه یک بیپ — و یک درخششِ ملایم یک اکتاو بالاتر که همان چیزی است که صدا
را «مالِ ما» می‌کند نه «یک چایمِ پیش‌فرض». ۴۰ میلی‌ثانیه fade-out که روی اسپیکرِ ارزان تقّه نزند.

**چرا سنتز شد، نه یک فایلِ آماده:** مجوز (نمونه‌های آماده کارِ دیگران‌اند و این یک APK تجاری است)، حجم، و
**تکرارپذیری** — `scripts/gen-notification-sound.mjs` منبع است؛ عددی را عوض کن، اجرا کن، باینریِ res/raw
بیت‌به‌بیت همان می‌شود. تنها راهِ شرافتمندانه برای زندگیِ یک فایلِ باینری در گیت همین است، و تست آن را
اجرا و هش را مقایسه می‌کند.

**چرا WAV و نه OGG/MP3:** اندروید PCM را از res/raw بدون مذاکره با هیچ دیکدری پخش می‌کند، و این مخزن در
زنجیره‌ابزارش انکودری ندارد — «تولید ogg» یعنی تحویلِ باینری‌ای که هیچ‌کس اینجا نمی‌تواند بازسازی‌اش کند.
۱۱۶ کیلوبایتِ یک‌باره قیمتِ منصفانه‌ای برای تضمینِ پخش است.

### صدا روی اندروید ۸+ مالِ «کانال» است، نه نوتیفیکیشن

این همان جایی است که این کار با شکستِ خاموش روبه‌رو می‌شد. یک `setSound()` روی بیلدری که به کانالی با صدای
سیستم پست شود **اصلاً نادیده گرفته می‌شود**؛ و صدای یک کانال (مثل اهمیتش) بعد از ساخته شدن **هرگز عوض
نمی‌شود**. یعنی یک نصب که کانالش را با صدای پیش‌فرض ساخته باشد، برای همیشه با همان می‌ماند — حتی بعد از
هر آپدیتی. برای همین شناسه‌ها نسخه‌دارند: `fbt_alerts_v1` و `fbt_updates_v1`.

زنجیره، سرتاسر:

| حلقه | فایل | چه می‌کند |
|---|---|---|
| صدا | `res/raw/fbt_notification.wav` | باینریِ تولیدشده |
| کانال‌ها | `MainActivity.createNotificationChannels()` | هر دو کانال را **در onCreateِ هر اجرا** با این Uri می‌سازد: alerts با `IMPORTANCE_HIGH` (همان که باعث می‌شود نوتیفیکیشن واقعاً از بالا بیاید)، updates با `IMPORTANCE_DEFAULT` |
| پیش‌فرضِ FCM | `AndroidManifest.xml` | `default_notification_channel_id = fbt_alerts_v1` — وگرنه SDK کانالِ `fcm_default_channel` را خودش با صدای سیستم می‌سازد |
| پیام | `server/fcm.js` | `sound: 'fbt_notification'` (قبلاً در هر دو شاخهٔ یک ترنری، رشتهٔ `'default'` بود) و `channelId` متناظر |
| محلی | `src/lib/notify.js` | همان کانال و همان صدا (بدون پسوند — اندروید با نامِ پایه حل می‌کند) |
| پیش‌فرضِ پلاگین | `capacitor.config.json` | `LocalNotifications.sound` برای نوتیفیکیشنی که چیزی نام نبرده |

نامِ کانال‌ها در `values/` و `values-fa/` هم ترجمه شده — چون کاربر آن‌ها را در
تنظیمات → برنامه‌ها → FBT Swap → نوتیفیکیشن‌ها می‌بیند و می‌تواند یک کانال را بدون از دست دادنِ دیگری
خاموش کند، که دقیقاً دلیلِ وجودِ دو کانال است.

**تست:** `test/notification-sound-probe.mjs` (واردِ `npm test` شده) — باینری واقعاً PCM و غیرِ صامت است،
محدودهٔ شدت، بازتولیدِ بیت‌به‌بیت، Uri در `MainActivity`، `IMPORTANCE_HIGH` برای هشدارها، متادیتایِ
مانیفست، `channelId` در `server/fcm.js`، و اینکه دیگر هیچ payloadای `'default'` نمی‌خواهد.

---

## تست‌ها

```
node test/notification-sound-probe.mjs       ✓ (بدون دستگاه، بدون شبکه)
npx vitest run test/network-select.test.jsx  ✓ ۱۹ تست
npx vitest run test/proxy-advice.test.jsx    ✓ ۹ تست   (npm run test:welcome)
npm run test:wallet-connection               ✓ ۳۴ تست
first-launch flow (splash → welcome → onboarding → guide)  ✓
npx vite build                               ✓
```
