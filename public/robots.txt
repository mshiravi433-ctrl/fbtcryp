# FBT Swap public pages are crawlable by search and AI crawlers.
# A single wildcard group also keeps *every* bot out of /api/. Separate
# bot-specific groups with only Allow: / would override this exclusion.
User-agent: *
Allow: /
Disallow: /api/

# Lists only real HTML pages; hash-routed app screens share the root document.
Sitemap: https://fbtswap.ir/sitemap.xml
