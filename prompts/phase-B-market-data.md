# قوس B — دادهٔ بازار و تحلیل (فازهای ۵۸–۶۲)

> نسخهٔ مفصل قوس B. سند جامع: `phase-51-100-master-prompt.md`.
> در هر session فقط این قوس (یا بخشی از آن) را انجام بده و برای همان یک PR باز کن.

## ۵۸. رژیم بازار زنده
اسنپ‌شات کش‌شده، رژیم بازار نیست. `src/lib/intent-ai/marketRegime.js` (`detectMarketRegime`) با قیمت واقعی از `src/lib/api.js` تغذیه شود؛ فید مرده = honest-unavailable؛ رژیم از دادهٔ کهنه‌تر از `maxAgeHrs` ساخته نشود؛ در پاسخ‌های تحلیل چت دیده شود.

## ۵۹. هشدار قیمت → پیشنهاد اینتنت
هشدار، مجوز اجرا نیست. هشدار فقط **proposal** بسازد که کاربر صریحاً تأیید کند؛ مسیر پیشنهاد → صفحهٔ تأیید تعاملی موجود، بدون میان‌بُر؛ فید قطع = informed صادقانه.

## ۶۰. تحلیل قابل‌توضیح
حکم، استدلال نیست. `whyTransparency.js` (`whyThisDecision`) به دادهٔ واقعی وصل شود؛ هر توصیه «چرا»اش را با مبنای داده (منبع، زمان، اعداد) بیاورد؛ عدد جعل‌شده ممنوع — داده نیست، دلیل هم نیست.

## ۶۱. پیشرفت هدف زنده
شمارش معکوس، پیشرفت نیست. `goalProgress.js` با قیمت واقعی تغذیه شود و `GoalCountdown.jsx` با نوار پیشرفت واقعی گسترش یابد؛ بدون داده: پیشرفت honest-null است نه صفر جعلی.

## ۶۲. بک‌تست صادقانه
بک‌تست، وعده نیست. `strategyCompetition.js` روی دادهٔ تاریخی واقعی؛ هر نتیجه برچسب «شبیه‌سازی» + بازهٔ زمانی + مبنای داده؛ look-ahead/کمیسیون/اسلیپیج لحاظ و افشا شوند؛ ادعای سود آینده ممنوع.

## قیدها
- Fail-closed و صادق؛ هیچ تغییر قبلی revert نشود.
- probe به ازای هر فاز در `test/intent-ai/` + ثبت در `test/run.mjs`؛ `npm test` سبز.
- همهٔ متن‌های کاربرپسند فقط از طریق کلیدهای i18n (en/fa/ar)؛ هیچ کاراکتر فارسی/عربی در سورس پنل.
