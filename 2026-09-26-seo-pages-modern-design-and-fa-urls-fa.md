# بازطراحی کامل صفحات سئو + نشانی‌های انگلیسی + کتابخانهٔ مرجع — ۲۰۲۶-۰۹-۲۶

خواسته‌ها چهار تا بود: «تمام صفحات سئو قشنگ‌تر و مدرن‌تر بشوند، باکس‌ها و فاصله‌ها
درست باشند»، «آیکون‌ها انیمیشن بیشتر داشته باشند ولی آرام — مثل چراغ خطر چشمک
نزنند»، «بعد از .ir نشانی‌ها انگلیسی باشند تا بهتر ایندکس شوند»، و «همهٔ صفحات —
فارسی‌ها هم — در یک جای مرجع باشند که کاربر پیدایشان کند». در ادامه هر چهار، با عدد.

---

## ۱. نشانی‌های انگلیسی زیر دامنهٔ .ir

هر ۱۴ صفحهٔ فارسی که با اسلاگ عربی‌نویس منتشر شده بودند به مسیر انگلیسی زیر `/fa/`
منتقل شدند؛ لندینگ دوزبانه هم از `صرافی-غیرمتمرکز` به `decentralized-crypto-exchange`
رفت. نمونه:

| قبل | بعد |
|---|---|
| `/سواپ-ارز-دیجیتال` | `/fa/crypto-swap-without-kyc` |
| `/سرمایه-گذاری-در-ارز-دیجیتال` | `/fa/crypto-investing-yield-and-lending` |
| `/کارمزد-سواپ-ارز-دیجیتال` | `/fa/how-crypto-swap-fees-work` |
| `/وبلاگ` | `/fa/blog` |
| `/صرافی-غیرمتمرکز` | `/decentralized-crypto-exchange` |

محتوای صفحه‌ها همان است و `lang="fa" dir="rtl"` و hreflang دوطرفه سرِ جای خودش
مانده؛ فقط نشانی عوض شده. برای اینکه رتبهٔ موجودِ نشانی‌های قبلی از بین نرود، دو
لایهٔ ریدایرکت گذاشته شد:

1. **`vercel.json`** — پانزده ریدایرکت ۳۰۸ دائمی. این همان چیزی است که خزنده و
   کاربر در تولید می‌بینند.
2. **استاب ایستا** — `scripts/gen-landing.mjs` برای هر نشانی قدیمی یک سند کوچک
   `noindex` می‌سازد با `canonical` به نشانی جدید، یک `location.replace` و یک لینک
   دیدنی. این بیمهٔ میزبان‌هایی است که بدون تنظیمات Vercel فقط `dist/` را سرو
   می‌کنند. عمداً `meta refresh` نیست — رفرش لحظه‌ای همان الگوی «صفحهٔ دروازه» است
   که آزمون مسیربندی (و گوگل) جریمه‌اش می‌کند.

`submit-indexnow.mjs`، `llms.txt`، بلوک `<noscript>` صفحهٔ اصلی و جفت‌های `hreflang`
همگی با نشانی‌های تازه همگام شدند. یک ناسازگاری عددی هم در همین مسیر رفع شد: توضیح
صفحهٔ `crypto-swap-without-kyc` می‌گفت «14 networks» در حالی که متن صفحه و رجیستری
۱۷ شبکه است — توضیح متا به ۱۷ اصلاح شد.

## ۲. کتابخانهٔ صفحات — مرجع همهٔ صفحات

دو صفحهٔ تازه از جنس `library` اضافه شد: `/library` (انگلیسی) و `/fa/` (فارسی — که
هم‌زمان درگاه کل بخش فارسی است). هر دو از خودِ آرایهٔ `PAGES` رندر می‌شوند، پس
صفحه‌ای که تولید شود و در فهرست نیاید وجود ندارد. ساختار فهرست:

- یک کارت ویژه برای لندینگ اصلی با حاشیهٔ گرادیانی.
- چهار گروه موضوعی (سواپ و نگهداری · بازار و نمودار · سرمایه‌گذاری و بازده ·
  آموزش و راهنماها) با ۲۸ کارت — هر کارت با آیکون خود صفحه، برچسب زبان (فارسی/EN)،
  عنوان و توضیح.
- صفحه‌های هم‌زبانِ خود فهرست اول می‌آیند: در `/fa/` اول کارت‌های فارسی، در
  `/library` اول کارت‌های انگلیسی.
- داده‌ساختار `ItemList` که همهٔ نشانی‌ها را یک‌جا به خزنده می‌دهد.

دسترسی به این مرجع از همه‌جا ممکن است: سومین جزءِ بردکرامب هر صفحه، پیوند
«همهٔ صفحات» در هدر، پیوند در فوتر، لینک‌های تازه در بلوک `<noscript>` صفحهٔ اصلی،
در `llms.txt` و در فوتر لندینگ اصلی.

## ۳. قالب بصری جدید — همین یک قالب برای هر ۳۱ صفحه

قالب مشترک (`render()` در `scripts/gen-landing.mjs`) از صفر نوشته شد:

- **باکس‌ها و فاصله:** یک مقیاس فاصلهٔ واحد (`--gap`) بین همهٔ پنل‌ها، شعاع‌های
  ۲۰/۲۸، پدینگ `clamp(24px, 4.4vw, 44px)`، و چیدمان ستونی با `grid` به‌جای
  `margin-top`های تکی — یعنی فاصله‌ها همه‌جا یک اندازه‌اند.
- **آیکون‌ها:** ۲۲ آیکون خطیِ دست‌کشیدهٔ SVG داخل «چیپ»های اندازه‌ثابت (هیچ‌وقت
  جابه‌جایی چیدمان نمی‌سازند). هر کارت «در یک نگاه»، هر آیتم فهرست، هر سرفصل و
  خودِ هرو یک چیپ دارد. چهار رنگ‌بندی چرخشی (فیروزه‌ای/بنفش/لیمویی/صورتی) تا ردیف
  کارت‌ها شبیه مُهر تکراری نباشد.
- **انیمیشنِ آرام، نه چشمک:** چهار حرکت بی‌نهایت برای چیپ‌ها — شناور (۵٫۶ ثانیه)،
  تنفس هاله (۴٫۸ ثانیه)، مدارِ یک نقطهٔ کوچک (۹٫۵ ثانیه) و تاب خوردن (۶٫۶ ثانیه) —
  همه فقط با `transform` و `opacity`، همه کند و سینوسی. هیچ انیمیشن `steps()` یا
  روشن/خاموشِ یک‌هرتزی وجود ندارد؛ حتی نقطهٔ «زنده» به‌جای چشمک، بین کدریت ۰٫۵۵ و ۱
  نفس می‌کشد. در هرو هم یک مدال شناور با دو حلقهٔ چرخان ۴۴ و ۶۲ ثانیه‌ای و سه
  ستارهٔ چشمک‌نرم نشسته است.
- **بودجهٔ حرکت، مثل قبل:** زیر `@media (hover: none), (max-width: 999px)` همهٔ
  حرکت‌های بی‌نهایت روی فریم اول می‌ایستند، `blur` و `backdrop-filter` حذف می‌شوند؛
  روی موبایل همان تصویر می‌ماند ولی فریم‌ها خرج اسکرول می‌شوند نه پس‌زمینه.
  `prefers-reduced-motion` هم همه‌چیز را خاموش می‌کند. تشخیص user-agent هیچ‌جا نیست.
- **ظهور با اسکرول:** پنل‌ها با `IntersectionObserver` هنگام ورود به دید ظاهر
  می‌شوند؛ بدون جاوااسکریپت، همان ظهورِ بارگذاریِ قدیمی اجرا می‌شود، پس خزندهٔ
  بدون JS هیچ‌چیز را از دست نمی‌دهد.
- **سازگاری با تست‌های موجود:** همهٔ نشانه‌هایی که آزمون‌ها در قالب جست‌وجو می‌کنند
  (`class="cta" href=`، `<details>`، `FAQPage`، `social-card.png`،
  `prefers-reduced-motion`، `@keyframes rise-in`، `fact-card`، `ambient-grid`) سرِ
  جای خودشان هستند.

## ۴. اندازه‌گیری‌ها و تست‌ها

- `npm run test:landing` → **۱۱۵ / ۰** (شامل زبان، دادهٔ زنده، شکستِ صادقانه،
  راهنماها، هاب‌ها و شمارش نقشهٔ سایت).
- `npm run test:seo` → **۹ / ۰** (سایت‌مپ ↔ فایل‌ها، یکتایی عنوان/توضیح، تقابل
  hreflang، جفت بودن دو کتابخانه، صفحه‌های بازار و توسعه‌دهندگان).
- `node test/wiring.mjs` → **۲۷۰۹ ادعا / ۱۴ خطا**؛ روی `main` دقیقاً **۲۷۰۸ / ۱۴**
  است. تنها تفاوتِ تعداد، سه زیرادعای تازهٔ بلوک `<noscript>` است؛ مجموعهٔ خطاها
  با `main` یکسان است — هیچ خطای تازه‌ای اضافه نشد.
- ۳۱ صفحهٔ استاتیک + ۱۵ استاب ریدایرکت تولید شد؛ سایت‌مپ ۳۲ نشانی دارد.

## ۵. چیزی که دست نخورد

`src/`، باندل برنامه، سرور و هیچ‌یک از صفحات داخل اپ تغییر نکرد. همهٔ تغییرات در
`scripts/` (ژنراتور و قالب)، چند فایل تست، `index.html` (بلوک `<noscript>` و پیوند
کتابخانه)، `public/llms.txt` و `vercel.json` (ریدایرکت‌ها) است.
