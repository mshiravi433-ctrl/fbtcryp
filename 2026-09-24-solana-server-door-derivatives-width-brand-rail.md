# وام سولانا — درب دوم (سرورِ خودمان)، تالار مشتقهٔ عریض‌تر، و نوار برند روی هر اسکرین‌شات

> ۲۰۲۶-۰۹-۲۴ · شاخه: `arena/01a0d085-fbtcryp` · دو commit: `7cb7eab` (سولانا) و `eeebb0e` (مشتقه + برند) · هدف: merge به `main` → deploy خودکار Vercel روی `https://fbtswap.ir`

## خلاصهٔ یک‌خطی

سه گزارش، سه راه‌حل در یک PR: ❶ تب سولانا در صفحهٔ وام دوباره با ۹ میزبانِ ردکننده بسته بود — حالا بازار Kamino از **سرورِ خود اپ** هم خوانده می‌شود (دو درب با هم *رقص* می‌کنند، نه پشت‌سرهم)؛ ❷ تالار مشتقه فشرده بود — ۳۲ پیکسل حاشیهٔ تکراری حذف شد و ستون صفحه ۴۸ پیکسل عریض‌تر شد؛ ❸ روی اسکرین‌شات‌ها لوگو و آدرس سایت نبود — یک نوار برند ثابت پایین همهٔ صفحه‌ها + دکمهٔ «اسکرین‌شات و اشتراک‌گذاری».

---

## ❶ وام ← تب سولانا: «RPC سولانا یا بازار Kamino خوانده نشد»

### گزارش

> «در وام تب سولنا بهم خورده دوباره» — با فهرست ۹ میزبان: `solana.leorpc.com` غیرقابل دسترسی؛ رلهٔ خود اپ پاسخ داد ولی «پاسخی که نتوانستیم استفاده کنیم»؛ `publicnode`، `magicblock`، `api.mainnet.solana.com`، `api.mainnet-beta.solana.com` → ۴۰۳ مسدود؛ `rpc.solanatracker.io` و `solana.drpc.org` پاسخ دادند ولی بی‌فایده؛ `onfinality` → ۴۲۹.

### چرا با همان معماری قبلی درست‌شدنی نبود

خواندن بازار Kamino یک `getProgramAccounts` روی بازار اصلی است: **≈۱٫۵ تا ۲ مگابایت** (هر Reserve ‏۸۶۱۶ بایت). نودهای رایگان یا این متد را رد می‌کنند (۴۰۳)، یا سقف اندازهٔ پاسخ دارند (۵۰۲/۴۲۹)، یا از یک IP موبایلِ فیلترشده اصلاً در دسترس نیستند. یعنی شکستِ این صفحه **انتظار می‌رفت**، نه استثنا. تنها راهِ باقی‌مانده این بود که همان خواندن جایی انجام شود که نودها جواب می‌دهند: سرورِ خودمان.

| تشخیص | نتیجه |
|---|---|
| «رله پاسخ داد ولی پاسخش به کار نیامد» | رلهٔ ما `-32004 / 502` بالادست را **بدون نام** برمی‌گرداند و کلاینت آن را «نود پاسخ داد ولی نشد استفاده کرد» می‌نامید — جمله‌ای که کاربر را دنبال RPC بهتر می‌فرستد در حالی که راه‌حل، درب دوم است |
| ۴۰۳های مستقیم نودها | درست و قابل اقدام: «مسیر شبکهٔ تو مسدود است؛ RPC خودت را بگذار» |
| gPA بزرگ | سمت سرور با `dataSlice` به **~۲۰ کیلوبایت** آدرس خالی تبدیل می‌شود و بعد `getMultipleAccountsInfo` تکه‌تکه (۲۵ تا) |

### چه شد — «درب دوم» سمت سرور

| تغییر | فایل |
|---|---|
| موتور Kamino سمت سرور: `market` / `transaction` / `transaction/:signature` / `status`، با کش TTL، بودجهٔ ساخت تراکنش، و fallback «آدرس‌های خالی با dataSlice» برای شمارش ذخیره‌ها | `server/solanaLending.js` (جدید) |
| mount شدن روتر **قبل از** `/api/lending` و بعد از rate-limiter | `server/app.js` |
| دو درب **رقص** می‌کنند (نه زنجیره): مرورگر و سرور همزمان، سرور یک ضربان (۱٫۲ ثانیه) عقب‌تر — و وقتی hint ذخیره‌شده می‌گوید نودهای عمومی این مسیر رد کرده‌اند، آن ضربان **صفر** می‌شود تا کاربر بلافاصله ۱٫۲ ثانیه هوای مرده نخورد | `src/lib/solanaLending.js` |
| لایهٔ fetch کلاینتِ درب سرور؛ کدِ خودِ سرور بر کدِ ساخته‌شده از status اولویت دارد (جز ۴۰۴ = «این استقرار هنوز این درب را ندارد») | `src/lib/solanaLendingServer.js` (جدید) |
| کد جدید `RELAY_UPSTREAM_UNAVAILABLE` — **فقط** وقتی هیچ وضعیت HTTP در پاسخ نیست؛ ۴۰۳ یا ۴۲۹ِ فورواردشده همچنان `RPC_BLOCKED` / `RPC_RATE_LIMITED` می‌ماند چون آن‌ها قابل اقدام‌اند | `src/lib/solanaLending.js` |
| پنل می‌گوید داده از کدام درب آمد («از راه سرورِ خودِ اپ خوانده شد») و وقتی هر دو درب بسته‌اند، حکم درب دوم را هم زیر فهرست میزبان‌ها می‌آورد | `src/components/SolanaLendingPanel.jsx` |
| ۳۳ کدِ ممکن × ۱۲ زبان، به‌علاوهٔ `loan.rpc.serverHost` / `serverTried` / `loan.solana.serverDoor` | `src/i18n/locales/*` |

**امنیت (بند ۳۰ دست‌نخورده):** سرور هرگز امضا و هرگز برادکست نمی‌کند. فقط تراکنش را می‌سازد و base64 را برمی‌گرداند؛ کیف پولِ خود کاربر امضا و ارسال می‌کند — یعنی حتی روی شبکه‌ای که ما را ۴۰۳ می‌کند، تراکنش از مسیر خود کاربر کامل می‌شود. یک تست صریحاً بررسی می‌کند که در `server/solanaLending.js` هیچ `sendTransaction` / `sendRawTransaction` / `signTransaction` وجود ندارد.

**عملیات:** اولین فراخوانیِ بازار ممکن است چند ثانیه طول بکشد (بارگذاری SDK + خواندن بازار) و بعد تا TTL کش می‌شود. کلیدها: `SOLANA_LENDING_SERVER_ENABLED` (خاموش‌کردن اضطراری درب)، `SOLANA_LENDING_*_TTL_MS`، `SOLANA_LENDING_*_TIMEOUT_MS`، `SOLANA_LENDING_CHUNK`، `SOLANA_LENDING_BUILD_BUDGET`. اگر هیچ نودی فهرست ذخیره‌ها را نداد، می‌توان با `SOLANA_KAMINO_RESERVE_ADDRESSES` فهرست را seed کرد.

---

## ❷ تالار مشتقه: «همهٔ جدول‌ها و باکس‌ها فشرده‌اند»

### اول، پاسخ پرسشِ درآمدزایی بدون لینک خارجی

**بله — ممکن است، و همین حالا هم هست.** صفحهٔ تالار مشتقه هیچ `href`، `window.open` یا `target="_blank"` ندارد؛ دو دکمهٔ پایانی آن به صفحهٔ داخلی `/trade` (خرید/معامله) و `/earn` (کسب درآمد) می‌روند و زیرشان نوشته «معامله و کسب درآمد در همین برنامه — بدون لینک بیرونی». درآمد از همان مسیر داخلی می‌آید: کارمزد سواپ **۷۰ bps** (`FEE_BPS_DEFAULT = 70`). تنها چیزی که ساخته نشده، کارمزد سازندهٔ پوزیشن‌های perp است (بازار مشتقه الان فقط تابلوی قیمت/بازده/فاندینگ است و اجرای پوزیشن از راه `/trade`).

### دو علت فشردگی، و فقط یکی‌شان «عرض» است

| علت | اندازه |
|---|---|
| صفحهٔ سهام خودش یک `.page` است (۱۶ پیکسل حاشیه) و Ostium و تالار مشتقه **بدون** `embedded` داخلش رندر می‌شدند، پس هرکدام یک `.page` دوم می‌ساختند | ۳۲ پیکسل حاشیهٔ مرده در هر طرفِ یک گوشی ۳۹۰ پیکسلی — به‌علاوهٔ همان باگِ `motion` تودرتو که کامنتِ خودِ `PageTransition` هشدارش را داده (جدِ position:fixed عوض می‌شود) |
| از ۶۰۰ پیکسل به بالا `.app-shell` یک ستون ثابت است (۶۰۰/۶۸۰/۷۶۰) که برای «یک ستون ردیف مالی» تنظیم شده، نه برای تابلویی با پنج ستون عددِ اندازه‌گیری‌شده + نمودار عمق + اردربوک | سقفِ عرض |

### چه شد

- `Stocks.jsx` حالا هر دو تب را `embedded` رندر می‌کند و `Ostium.jsx` هم این prop را می‌پذیرد → **۳۲ پیکسل برگشت** در همهٔ اندازه‌ها.
- تالار با کلاس `body.hall-wide` از پوسته ۴۸ پیکسل بیشتر می‌خواهد و هنگام unmount پس می‌دهد: `۵۲۰→۵۶۸`، `۶۰۰→۶۴۸`، `۶۸۰→۷۲۸`، `۷۶۰→۸۰۸`. `bottom-nav` و نوار برند هم در همان بازه‌ها هم‌عرض می‌شوند (وگرنه میلهٔ ثابت از ستون جدا می‌افتد — همان «نصفش میاد»).
- چهار بازهٔ رسانه‌ای **بدون هم‌پوشانی** نوشته شد، چون CSS یک صفحهٔ lazy جایگاهش در bundle دستِ ما نیست؛ با `min-width` خالی، یک جابه‌جایی ترتیب می‌توانست صفحه را ۴۸ پیکسل *باریک‌تر* کند.
- خودِ ردیف‌ها ۱۲ پیکسل دیگر گرفتند (حاشیهٔ داخلی سالن ۱۶→۱۰) در حالی که پس‌زمینه/aurora همچنان تا لبهٔ ستون می‌رود.

---

## ❸ نوار برند + دکمهٔ «اسکرین‌شات و اشتراک‌گذاری»

> «وقتی در سایت و اپ اسکرین‌شات گرفته می‌شود، لوگو و نام سایت (یا آدرس سایت) پایین صفحه باشد. باید مدرن باشد و سایت را درست معرفی کند. دکمهٔ اشتراک‌گذاری اسکرین‌شات هم اضافه کن.»

**چرا ثابت (fixed) و نه انتهای محتوا:** اسکرین‌شات را **سیستم‌عامل** می‌گیرد، از آنچه همان لحظه روی صفحه است. برندی که بعد از ۴۰ کارت بیاید، برندی است که تقریباً هیچ‌وقت داخل عکس نیست. پس یک نوار ۲۸ پیکسلی بالای نویگیشن پایین pinned شد: سکهٔ برند + `FBT Swap` + دامنه، با همان سه رنگ گرادیان به‌صورت یک خطِ موییِ بالا.

- روی همهٔ مسیرهایی که nav دارند؛ در `/pay/:code` حالت `bare` (لبهٔ پایین، چون nav ندارد)؛ در `/intent` اصلاً رندر نمی‌شود (آن صفحه تب‌های پایینِ خودش را دارد).
- **آدرس هرگز تایپ نشده**: از `brandDomain()` می‌آید که از `publicAppUrl()` مشتق می‌شود و هر origin غیررسمی را رد می‌کند. این دقیقاً همان دفاعی است که بعد از ماجرای `lawpoetics.ir` نوشته شد؛ نوارِ برندی که آدرس اشتباه بگوید از نبودش بدتر است.

**دکمهٔ اشتراک = capture → brand → share:**

| پله | چه می‌شود |
|---|---|
| capture | ‏`html-to-image` فقط گرهٔ محتوای همان صفحه را raster می‌کند؛ نویگیشن پایین، خودِ نوار برند، کپسولِ pull-to-refresh، toastها و هر چیزی که `data-screenshot-ignore` دارد **بیرون** می‌مانند (دکمهٔ اشتراک هم داخل عکسِ خودش نمی‌آید) |
| brand | یک canvas عکس را به همان بخشی که روی صفحه بود crop می‌کند و نوار برند را **زیرش می‌کشد** — کشیده می‌شود نه عکاسی، چون المان fixed در cloneٔ DOM در جایی که کاربر دید نمی‌نشیند. اگر canvas در دسترس نباشد، همان عکسِ بدون نوار به اشتراک می‌رود |
| share | همان نردبانِ `lib/share.js`: اپ بسته‌بندی‌شده (Capacitor فایل PNG را در cache می‌نویسد و **فایل** را به شیت اندروید می‌دهد) ← Web Share API با `File` (و بدون `url` کنارش، چون سافاری این ترکیب را رد می‌کند) ← جایی که هیچ‌کدام تصویر را نمی‌برند: **ذخیرهٔ تصویر + کپی لینک** و گفتنِ صادقانهٔ همین |

هیچ‌وقت «هیچ» اتفاق نمی‌افتد: اگر capture شکست بخورد، **لینک همان صفحه** با `siteShareUrl()` (همراه کد رفرال خود کاربر، مثل صفحهٔ Shop) به اشتراک می‌رود. بستن شیت یک **تصمیم** است نه خطا (`DISMISSED`) و toast قرمز نمی‌سازد.

هزینه: ‏۷ کیلوبایت به first-paint (خودِ نوار + `lib/brand.js`). موتور capture و `html-to-image` با `import()` **در لحظهٔ کلیک** بار می‌شوند، نه در باز شدن اپ.

---

## چیزهایی که سر راه پیدا و درست شدند

| مورد | وضعیت |
|---|---|
| ۳ تستِ رله که hardcode کرده بودند «solanatracker اولِ فهرست است» | از قبل قرمز بودند؛ حالا میزبانِ ردکننده را از `relayUpstreams()` می‌گیرند (چیزی که زیر تست است *راه رفتن* است، نه جای یک فروشنده در فهرست) |
| ۷ کلید `t()` که در هیچ locale وجود نداشتند — ۳ تای آن‌ها با `defaultValue` **فارسی** | یعنی دکمه‌ای که به کاربر اسپانیایی «خرید / معامله» نشان می‌داد. `loan.rpc.showMore` در هر ۱۲ زبان اضافه شد؛ سه namespace که فقط en/fa دارند (`derivatives`، `solana.picker`، `intentOS.proof`) در همان دو پر شدند و بقیه مثل قبل به انگلیسی برمی‌گردند |
| `ASSET_NOT_SUPPORTED` در ۸ زبان و `UNKNOWN_ACTION` در zh نبود | توسط تستِ پوششِ واژگان پیدا شد؛ پر شدند |
| ‏`npm install` قفلِ پلتفرمِ `package-lock.json` را شکست (`optional: true` از `fsevents` پاک شد) | `node ci/lock-platform-guard.mjs` دوباره اجرا شد — وگرنه build اندروید روی رانر لینوکس با `EBADPLATFORM` می‌مرد |
| لوگوی برند در دو جا کپی می‌شد | به `src/components/BrandMark.jsx` منتقل شد؛ assertion مربوطه در `test/wiring.mjs` هم به همان ماژول اشاره می‌کند |

---

## تست‌ها

۶۲ تست جدید، همه سبز:

| فایل | تعداد | چه چیزی را قفل می‌کند |
|---|---|---|
| `test/solana-lending-server-door.test.js` | ۱۱ | نیمهٔ سرور: مسیرهای router، fallback شمارش ذخیره‌ها، شکل `{id, transaction, versioned}`، و «هرگز امضا/برادکست نمی‌کند» |
| `test/solana-lending-doors.test.js` | ۱۴ | نیمهٔ کلاینت: رقصِ دو درب، skip سریع روی مسیرِ مسدودِ شناخته‌شده، گزارش هر دو درب، ۴۰۴ = `SERVER_ENDPOINT_MISSING`، `-32004` ≠ «پاسخِ بی‌فایده»، و **۳۳ کد × ۱۲ زبان** از هر دو طرف (کد باید هنوز در سورس تولید شود + جمله باید وجود داشته باشد) |
| `test/solana-lending-panel-doors.test.jsx` | ۴ | پنلِ واقعی mount می‌شود: «از راه سرور خوانده شد»، حکمِ درب دوم زیر فهرست میزبان‌ها، و **ادعا نکردنِ** دربی که اصلاً زده نشده |
| `test/derivatives-hall-width.test.jsx` | ۸ | یک `.page` نه دو تا؛ `body.hall-wide` هنگام mount/unmount؛ توافق دو stylesheet در هر چهار بازه (سالن همیشه عریض‌تر، هرگز باریک‌تر)؛ هم‌عرض شدن nav |
| `test/brand-rail-screenshot.test.jsx` | ۲۵ | آدرس از ماژول هویت می‌آید نه از literal؛ فیلتر capture واقعاً روی المان‌ها اعمال می‌شود؛ هر پلهٔ اشتراک (native/web/download/link) همان را می‌گوید که اتفاق افتاد؛ `DISMISSED` هرگز toast خطا نمی‌سازد؛ هندسهٔ نوار = هندسهٔ nav در هر breakpoint؛ ۹ کلید × ۱۲ زبان |

**وضعیت مجموعه:** ‏۹۹۲ تست، ۲۲ قرمز — دقیقاً همان ۲۲ تایِ قبل از این تغییرات (با `git stash` روی commit پایه مقایسه شد) و ۳ تستِ قرمزِ قدیمیِ رله حالا سبز. `npm run build` سبز. ‏`test/wiring.mjs`: ‏۱۴ → ۱۳ قرمز (کلیدهای ترجمه سبز شد؛ بقیه از قبل قرمز بودند). بودجهٔ bundle از قبل قرمز بود (۱۵۵۵ کیلوبایت در برابر سقف ۱۳۶۰) و این PR ‏۷ کیلوبایت به آن اضافه کرد.

---

## چگونه ببینید

1. ‏`npm run dev` ← صفحهٔ **وام ← تب سولانا**: اگر نودهای عمومی رد کنند، داده از درب سرور می‌آید و زیر عنوان می‌نویسد «از راه سرورِ خودِ اپ خوانده شد — گره‌های عمومی این مسیرِ شبکه را رد کردند».
2. ‏**سهام ← تالار مشتقه**: جدول‌ها ۴۴ پیکسل بازتر؛ روی دسکتاپ ستون ۴۸ پیکسل عریض‌تر و nav/نوار برند هم‌عرض با آن.
3. هر صفحه‌ای: نوار برند بالای نویگیشن پایین؛ دکمهٔ **اشتراک** روی گوشی شیتِ خودِ سیستم را با تصویرِ برنددار باز می‌کند، و روی دسکتاپ تصویر را ذخیره و لینک را کپی می‌کند (و همین را می‌گوید).
4. برای خاموش‌کردن اضطراری درب سرور: ‏`SOLANA_LENDING_SERVER_ENABLED=false`.
