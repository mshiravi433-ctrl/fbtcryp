/** Future FBT Protection Pool accounting. Disabled by default and intentionally
 * free of underwriting/payout activation logic. */
export const POOL_STATUS = 'DISABLED_PENDING_LEGAL_SECURITY_REVIEW';
export class ProtectionPoolAccounting { constructor(values = {}) { Object.assign(this, { totalCapital: 0, availableCapital: 0, lockedCapital: 0, claimsReserve: 0, operationalReserve: 0, premiumIncome: 0, claimsPaid: 0, netResult: 0, capitalUtilization: 0, lossRatio: 0, solvencyRatio: 0, ...values }); this.recalculate(); } recalculate() { this.netResult = this.premiumIncome - this.claimsPaid; this.capitalUtilization = this.totalCapital ? this.lockedCapital / this.totalCapital : 0; this.lossRatio = this.premiumIncome ? this.claimsPaid / this.premiumIncome : 0; this.solvencyRatio = this.totalCapital ? (this.availableCapital + this.claimsReserve + this.operationalReserve) / this.totalCapital : 0; return this; } }
export class PoolSolvencyEngine { constructor({ minimumReserveRatio = 0.2 } = {}) { this.minimumReserveRatio = minimumReserveRatio; } evaluate(accounting) { const a = accounting.recalculate(); const paused = a.solvencyRatio < this.minimumReserveRatio; return { availableCapital: a.availableCapital, eligibleReserves: a.claimsReserve + a.operationalReserve, outstandingLiabilities: a.lockedCapital, availableSolvency: a.availableCapital + a.claimsReserve + a.operationalReserve - a.lockedCapital, solvencyRatio: a.solvencyRatio, newCoverage: paused ? 'PAUSED' : 'AVAILABLE', reason: paused ? 'SOLVENCY_BELOW_CONFIGURED_THRESHOLD' : null }; } }
export function poolEnabled(config = {}) { return config.FBT_PROTECTION_POOL_ENABLED === true || config.enabled === true; }
export const POOL_SAFETY_LIMITS = Object.freeze(['minimumReserveRatio','maximumExposurePerProtocol','maximumExposurePerChain','maximumExposurePerProvider','maximumExposurePerIncident','maximumDailyClaims','maximumAggregateCoverage']);
