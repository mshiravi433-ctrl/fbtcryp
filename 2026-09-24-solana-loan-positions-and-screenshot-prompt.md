# وام سولانا: موقعیت‌ها برای کیف پولِ تازه، خواندنِ تازه بعد از تراکنش، باکسِ مقدارِ درست — و «اسکرین گرفتید؟» به‌جای نوارِ همیشگی

> ۲۰۲۶-۰۹-۲۴ · شاخه: `arena/01a0d100-fbtcryp` · هدف: merge به `main` ← deploy خودکار روی `https://fbtswap.ir`

## ❶ وام ← سولانا

### گزارش
> «مطمئن شو سولانا وام کار می‌کند، به کیف پول وصل می‌شود و موقعیت‌ها را نشان می‌دهد. باکس ورود مقدار خیلی کوچک و تخم‌مرغی است.»

### آنچه پیدا شد

| مشکل | ریشه | اصلاح |
|---|---|---|
| کیف پولی که هنوز در Kamino سپرده نگذاشته (حالت معمول برای کاربر تازه) به‌جای «موقعیتی ندارید» پیامِ **«RPC خوانده نشد · تلاش دوباره»** و خط‌تیره می‌دید؛ و «وام‌گیری» روی آن `RPC_ERROR` می‌داد به‌جای «اول وثیقه بگذارید» | `getUserVanillaObligation` در klend-sdk برای کیف پولِ بدون obligation **throw می‌کند** («Could not find vanilla obligation.») و هر سه جای کد آن را خطای شبکه می‌خواندند | تابع `readVanillaObligation` در `src/lib/solanaLending.js`: این جملهٔ SDK یعنی «هنوز obligation ندارد» ← `null`؛ فقط خطای واقعیِ شبکه throw می‌شود. هر سه فراخوانی (serializer مرورگر، سازندهٔ تراکنش مرورگر، سازندهٔ تراکنش سرور) از آن می‌گذرند |
| بعد از واریز، موقعیت تا ۲۰ ثانیه عوض نمی‌شد | درِ سرور snapshot را ۲۰ ثانیه کش می‌کند و refresh بعد از تراکنش همان کش را می‌گرفت | `refresh({ fresh: true })` بعد از هر تراکنشِ موفق ← `?refresh=1` روی `/api/lending/solana/market`. سازندهٔ تراکنشِ سرور هم preflight را روی snapshot تازه انجام می‌دهد |
| درِ مرورگر بازار را می‌خواند ولی موقعیت/موجودیِ کیف پول را نه، و همان برنده می‌شد | مسابقهٔ دو در فقط به «بازار آمد» نگاه می‌کرد | اگر snapshot مرورگر کیف پول را نخوانده باشد، تا ۶ ثانیه منتظر درِ سرور (که از قبل در راه است) می‌ماند و اگر سرور کیف پول را خوانده بود، آن را برمی‌دارد (`walletReadIncomplete`) |
| باکس مقدار «کوچک و تخم‌مرغی» | input و دکمه در یک ردیف flex بودند و `.btn` در کل اپ `width: 100%` است؛ دکمه تمام ردیف را می‌گرفت و input تا اندازهٔ padding خودش جمع می‌شد | باکس مقدار حالا ردیفِ خودش را دارد: تمام‌عرض، ارتفاع ۵۶px، عدد ۲۰px، نماد و آیکن دارایی کنارش (مثل پنل وام EVM)؛ دکمه تمام‌عرض زیرش. استایل در `src/styles/solana-loan.css` |
| تب «موقعیت‌های من» برای کیف پولِ وصل‌شده‌ای که موقعیت باز ندارد **خالی** بود | شرط نمایشِ پیام فقط `!account.ok` بود | پیام «موقعیتی نیست» در هر حالتِ بدون موقعیت؛ اگر کیف پول وصل نیست دکمهٔ «اتصال کیف پول» همان‌جا؛ ارزش دلاری هر موقعیت زیر مقدارش |
| — | — | اعداد فارسی/عربی (`۱۲٫۵`) در باکس مقدار به `12.5` تبدیل می‌شوند؛ وقتی کیف پول وصل نیست دکمهٔ اقدام خودش «اتصال کیف پول» است |

**محدودیتِ صادقانه:** سندباکسِ توسعه به RPCهای سولانا دسترسی شبکه ندارد، پس خواندنِ زندهٔ mainnet اینجا اجرا نشد؛ همه‌چیز با تست‌هایی که همان مسیرها را با SDK/بازارِ شبیه‌سازی‌شده اجرا می‌کنند قفل شده است.

## ❷ نوار برندِ بالای فوتر حذف شد ← «اسکرین گرفتید؟» فقط برای ۳۰ ثانیه

> «باکس بالای فوتر که دکمه اشتراک و آدرس سایت هست را پاک کن. وقتی کسی اسکرین گرفت یک بند برای ۳۰ ثانیه بیاد که بگه شما اسکرین گرفتید، می‌خواهید اشتراک بگذارید اسکرین‌شات را با واترمارک آدرس سایت — نه اینکه همیشه باشه.»

- `BrandRail.jsx` و `brand-rail.css` **حذف** شدند؛ فضای پایینی که برایش رزرو شده بود هم برگشت.
- `ScreenshotSharePrompt.jsx` (تازه): تا وقتی اسکرین‌شاتی تشخیص داده نشود **هیچ چیزی** رندر نمی‌کند. بعد از اسکرین‌شات بنری بالای نویگیشن می‌آید: «از صفحه اسکرین‌شات گرفتید — می‌خواهید آن را با واترمارک آدرس سایت fbtswap.ir به اشتراک بگذارید؟» + دکمهٔ «اشتراک با واترمارک» + ×، با نوار ۳۰ ثانیه‌ای که خالی می‌شود. اسکرین‌شاتِ دوباره ساعت را از نو شروع می‌کند.
- «اشتراک» همان زنجیرهٔ قبلی را اجرا می‌کند (`lib/screenShare.js`): صفحه دوباره رندر می‌شود و نوار برند (سکه + نام + آدرسِ canonical) **داخل تصویر کشیده می‌شود**. خودِ بنر داخل تصویر نمی‌آید.
- تشخیص (`src/lib/screenshotDetect.js`):
  - **اپ اندروید:** در Android 14+ از `Activity.ScreenCaptureCallback` (مجوز معمولی `DETECT_SCREEN_CAPTURE`، بدون دیالوگ)، و در اندرویدهای قدیمی‌تر از `ContentObserver` روی MediaStore فقط وقتی اپ جلوی صفحه است (بدون مجوزِ حافظه، تصویر هرگز باز نمی‌شود). هر دو ← رویداد `fbt:screenshot`.
  - **مرورگر دسکتاپ:** کلید `PrintScreen`، ‏⌘⇧3/4/5 در مک و ⊞⇧S در ویندوز (هر وقت مرورگر آن کلیدها را دریافت کند).
  - **مرورگر موبایل (سافاری/کروم روی گوشی):** وب هیچ رویدادی برای اسکرین‌شات ندارد — این محدودیتِ خودِ پلتفرم است و آنجا بنر نمی‌آید.
- ۴ کلید تازه (`share.shotTitle/shotBody/shotShare/shotClose`) × ۱۲ زبان.

## تست‌ها

| فایل | تعداد |
|---|---|
| `test/solana-loan-positions-amount.test.jsx` (تازه) | ۱۵ |
| `test/screenshot-prompt.test.jsx` (جایگزین `brand-rail-screenshot`) | ۲۹ |

کلِ مجموعهٔ vitest: ‏۱۰۱۱ تست، ۲۲ قرمز — همان ۲۲ تایِ قبلی (farm/protocol/intent، بی‌ارتباط). `walletconnect-stack-probe` سبز (۴۰۹/۴۰۹، از جمله جدولِ visibilityِ `onStart`/`onStop`)، `test/wiring.mjs` ‏۱۳ ← ۱۲ قرمز، `npm run build` سبز.
